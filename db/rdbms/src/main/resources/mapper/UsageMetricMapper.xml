<?xml version="1.0" encoding="UTF-8" ?>
<!--
  ~ Copyright Camunda Services GmbH and/or licensed to Camunda Services GmbH under
  ~ one or more contributor license agreements. See the NOTICE file distributed
  ~ with this work for additional information regarding copyright ownership.
  ~ Licensed under the Camunda License 1.0. You may not use this file
  ~ except in compliance with the Camunda License 1.0.
  -->
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="io.camunda.db.rdbms.sql.UsageMetricMapper">

  <resultMap id="UsageMetricStatisticsResultMap"
    type="io.camunda.db.rdbms.write.domain.UsageMetricDbModel$UsageMetricStatisticsDbModel">
    <constructor>
      <arg column="EVENT_TYPE" javaType="io.camunda.zeebe.protocol.record.value.UsageMetricRecordValue$EventType"/>
      <arg column="TENANT_ID" javaType="java.lang.String"/>
      <arg column="EVENT_VALUE" javaType="java.lang.Long"/>
    </constructor>
  </resultMap>

  <select id="usageMetricStatistics"
    parameterType="io.camunda.search.filter.UsageMetricsFilter"
    resultMap="UsageMetricStatisticsResultMap"
    statementType="PREPARED">
    SELECT EVENT_TYPE,
           TENANT_ID,
           SUM(EVENT_VALUE) as EVENT_VALUE
    FROM ${prefix}USAGE_METRIC
    WHERE 1 = 1
     <!-- TODO filter startDate - endDate -->
    GROUP BY EVENT_TYPE, TENANT_ID;
  </select>


  <insert id="insert" parameterType="io.camunda.db.rdbms.write.domain.UsageMetricDbModel">
    INSERT INTO ${prefix}USAGE_METRIC (START_TIME,
                                       END_TIME,
                                       TENANT_ID,
                                       EVENT_TYPE,
                                       EVENT_VALUE,
                                       PARTITION_ID,
                                       HISTORY_CLEANUP_DATE)
    VALUES (#{startTime, jdbcType=TIMESTAMP},
            #{endTime, jdbcType=TIMESTAMP},
            #{tenantId},
            #{eventType},
            #{value},
            #{partitionId},
            #{historyCleanupDate, jdbcType=TIMESTAMP})
  </insert>

  <update
    flushCache="true"
    id="updateHistoryCleanupDate"
    statementType="PREPARED">
    UPDATE ${prefix}USAGE_METRIC
    SET HISTORY_CLEANUP_DATE = #{historyCleanupDate, jdbcType=TIMESTAMP}
    WHERE PROCESS_INSTANCE_KEY = #{processInstanceKey}
  </update>

  <delete
    flushCache="true"
    id="cleanupHistory"
    statementType="PREPARED">
    <bind name="tableName" value="'USAGE_METRIC'"/>
    <include refid="io.camunda.db.rdbms.sql.Commons.historyCleanup"/>
  </delete>

</mapper>
