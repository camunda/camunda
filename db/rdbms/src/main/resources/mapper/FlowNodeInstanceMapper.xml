<?xml version="1.0" encoding="UTF-8" ?>
<!--
  ~ Copyright Camunda Services GmbH and/or licensed to Camunda Services GmbH under
  ~ one or more contributor license agreements. See the NOTICE file distributed
  ~ with this work for additional information regarding copyright ownership.
  ~ Licensed under the Camunda License 1.0. You may not use this file
  ~ except in compliance with the Camunda License 1.0.
  -->
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="io.camunda.db.rdbms.sql.FlowNodeInstanceMapper">

  <select id="count" resultType="java.lang.Long">
    SELECT COUNT(*)
    FROM ${prefix}FLOW_NODE_INSTANCE pi
    <include refid="io.camunda.db.rdbms.sql.FlowNodeInstanceMapper.searchFilter"/>
  </select>

  <!-- default search statement for databases supporting LIMIT/OFFSET-->
  <select id="search" parameterType="io.camunda.db.rdbms.read.domain.FlowNodeInstanceDbQuery"
    resultMap="io.camunda.db.rdbms.sql.FlowNodeInstanceMapper.searchResultMap">
    SELECT * FROM (
    SELECT
    FLOW_NODE_INSTANCE_KEY,
    LEGACY_ID,
    LEGACY_PI_ID,
    FLOW_NODE_ID,
    PROCESS_INSTANCE_KEY,
    PROCESS_DEFINITION_ID,
    PROCESS_DEFINITION_KEY,
    TYPE,
    STATE,
    START_DATE,
    END_DATE,
    TENANT_ID,
    TREE_PATH,
    INCIDENT_KEY,
    NUM_SUBPROCESS_INCIDENTS,
    CASE
    WHEN NUM_SUBPROCESS_INCIDENTS > 0 OR INCIDENT_KEY IS NOT NULL THEN TRUE
    ELSE FALSE
    END AS HAS_INCIDENT
    FROM ${prefix}FLOW_NODE_INSTANCE
    <include refid="io.camunda.db.rdbms.sql.FlowNodeInstanceMapper.searchFilter"/>
    ) t
    <include refid="io.camunda.db.rdbms.sql.Commons.keySetPageFilter"/>
    <include refid="io.camunda.db.rdbms.sql.Commons.orderBy"/>
    <include refid="io.camunda.db.rdbms.sql.Commons.paging"/>
  </select>

  <sql id="searchFilter">
    WHERE 1 = 1
    <if test="legacyId != null and !legacyId.isEmpty()">
      AND LEGACY_ID = #{legacyId}
    </if>
    <if test="legacyProcessInstanceId != null and !legacyProcessInstanceId.isEmpty()">
      AND LEGACY_PI_ID = #{legacyProcessInstanceId}
    </if>
    <!-- basic filters -->
    <if test="filter.flowNodeInstanceKeys != null and !filter.flowNodeInstanceKeys.isEmpty()">
      AND FLOW_NODE_INSTANCE_KEY IN
      <foreach collection="filter.flowNodeInstanceKeys" item="value" open="(" separator=", " close=")">#{value}</foreach>
    </if>
    <if test="filter.processDefinitionIds != null and !filter.processDefinitionIds.isEmpty()">
      AND PROCESS_DEFINITION_ID IN
      <foreach collection="filter.processDefinitionIds" item="value" open="(" separator=", " close=")">#{value}</foreach>
    </if>
    <if test="filter.processInstanceKeys != null and !filter.processInstanceKeys.isEmpty()">
      AND PROCESS_INSTANCE_KEY IN
      <foreach collection="filter.processInstanceKeys" item="value" open="(" separator=", " close=")">#{value}</foreach>
    </if>
    <if test="filter.processDefinitionKeys != null and !filter.processDefinitionKeys.isEmpty()">
      AND PROCESS_DEFINITION_KEY IN
      <foreach collection="filter.processDefinitionKeys" item="value" open="(" separator=", " close=")">#{value}</foreach>
    </if>
    <if test="filter.states != null and !filter.states.isEmpty()">
      AND STATE IN
      <foreach collection="filter.states" item="value" open="(" separator=", " close=")">#{value}</foreach>
    </if>
    <if test="filter.types != null and !filter.types.isEmpty()">
      AND TYPE IN
      <foreach collection="filter.types" item="value" open="(" separator=", " close=")">#{value}</foreach>
    </if>
    <if test="filter.flowNodeIds != null and !filter.flowNodeIds.isEmpty()">
      AND FLOW_NODE_ID IN
      <foreach collection="filter.flowNodeIds" item="value" open="(" separator=", " close=")">#{value}</foreach>
    </if>
    <if test="filter.incidentKeys != null and !filter.incidentKeys.isEmpty()">
      AND INCIDENT_KEY IN
      <foreach collection="filter.incidentKeys" item="value" open="(" separator=", " close=")">#{value}</foreach>
    </if>
    <if test="filter.hasIncident == true">
      AND (NUM_SUBPROCESS_INCIDENTS > 0 OR INCIDENT_KEY IS NOT NULL)
    </if>
    <if test="filter.hasIncident == false">
      AND (NUM_SUBPROCESS_INCIDENTS = 0 AND INCIDENT_KEY IS NULL)
    </if>
    <if test="filter.treePaths != null and !filter.treePaths.isEmpty()">
      AND TREE_PATH IN
      <foreach collection="filter.treePaths" item="value" open="(" separator=", " close=")">#{value}</foreach>
    </if>
    <if test="filter.tenantIds != null and !filter.tenantIds.isEmpty()">
      AND TENANT_ID IN
      <foreach collection="filter.tenantIds" item="value" open="(" separator=", " close=")">#{value}</foreach>
    </if>
  </sql>

  <resultMap id="searchResultMap" type="io.camunda.db.rdbms.write.domain.FlowNodeInstanceDbModel">
    <id property="flowNodeInstanceKey" column="FLOW_NODE_INSTANCE_KEY" javaType="java.lang.Long"/>
    <result property="legacyId" column="LEGACY_ID" javaType="java.lang.String"/>
    <result property="legacyProcessInstanceId" column="LEGACY_PI_ID" javaType="java.lang.String"/>
    <result property="flowNodeId" column="FLOW_NODE_ID" javaType="java.lang.String"/>
    <result property="processInstanceKey" column="PROCESS_INSTANCE_KEY" javaType="java.lang.Long"/>
    <result property="processDefinitionId" column="PROCESS_DEFINITION_ID" javaType="java.lang.String"/>
    <result property="processDefinitionKey" column="PROCESS_DEFINITION_KEY" javaType="java.lang.Long"/>
    <result property="type" column="TYPE" javaType="io.camunda.search.entities.FlowNodeInstanceEntity$FlowNodeType"/>
    <result property="state" column="STATE" javaType="io.camunda.search.entities.FlowNodeInstanceEntity$FlowNodeState"/>
    <result property="startDate" column="START_DATE" javaType="java.time.OffsetDateTime"/>
    <result property="endDate" column="END_DATE" javaType="java.time.OffsetDateTime"/>
    <result property="tenantId" column="TENANT_ID" javaType="java.lang.String"/>
    <result property="treePath" column="TREE_PATH" javaType="java.lang.String"/>
    <result property="incidentKey" column="INCIDENT_KEY" javaType="java.lang.Long"/>
    <result property="hasIncident" column="HAS_INCIDENT" javaType="java.lang.Boolean"/>
    <!--<result property="numSubprocessIncidents" column="NUM_SUBPROCESS_INCIDENTS" javaType="java.lang.Long"/>-->
  </resultMap>

  <insert
    id="insert"
    parameterType="io.camunda.db.rdbms.write.domain.FlowNodeInstanceDbModel"
    flushCache="true">
    INSERT INTO ${prefix}FLOW_NODE_INSTANCE (FLOW_NODE_INSTANCE_KEY, LEGACY_ID, LEGACY_PI_ID, FLOW_NODE_ID, PROCESS_INSTANCE_KEY,
                                    PROCESS_DEFINITION_ID, PROCESS_DEFINITION_KEY, TYPE, STATE,
                                    START_DATE, END_DATE, TENANT_ID, TREE_PATH, INCIDENT_KEY, NUM_SUBPROCESS_INCIDENTS)
    VALUES (#{flowNodeInstanceKey}, #{legacyId}, #{legacyProcessInstanceId}, #{flowNodeId}, #{processInstanceKey}, #{processDefinitionId},
            #{processDefinitionKey}, #{type}, #{state},
            #{startDate, jdbcType=TIMESTAMP}, #{endDate, jdbcType=TIMESTAMP}, #{tenantId},
            #{treePath}, #{incidentKey}, #{numSubprocessIncidents})
  </insert>

  <update
    id="updateStateAndEndDate"
    statementType="PREPARED"
    parameterType="io.camunda.db.rdbms.sql.FlowNodeInstanceMapper$EndFlowNodeDto"
    flushCache="true">
    UPDATE ${prefix}FLOW_NODE_INSTANCE
    SET STATE = #{state},
        END_DATE = #{endDate}
    WHERE FLOW_NODE_INSTANCE_KEY = #{flowNodeInstanceKey}
  </update>

  <update
    id="updateIncident"
    statementType="PREPARED"
    parameterType="io.camunda.db.rdbms.sql.FlowNodeInstanceMapper$UpdateIncidentDto"
    flushCache="true">
    UPDATE ${prefix}FLOW_NODE_INSTANCE
    SET INCIDENT_KEY = #{incidentKey}
    WHERE FLOW_NODE_INSTANCE_KEY = #{flowNodeInstanceKey}
  </update>

  <update
    id="incrementSubprocessIncidentCount"
    statementType="PREPARED"
    parameterType="java.lang.Long"
    flushCache="true">
    UPDATE ${prefix}FLOW_NODE_INSTANCE
    SET NUM_SUBPROCESS_INCIDENTS = NUM_SUBPROCESS_INCIDENTS + 1
    WHERE FLOW_NODE_INSTANCE_KEY = #{flowNodeInstanceKey}
  </update>

  <update
    id="decrementSubprocessIncidentCount"
    statementType="PREPARED"
    parameterType="java.lang.Long"
    flushCache="true">
    UPDATE ${prefix}FLOW_NODE_INSTANCE
    SET NUM_SUBPROCESS_INCIDENTS = NUM_SUBPROCESS_INCIDENTS - 1
    WHERE FLOW_NODE_INSTANCE_KEY = #{flowNodeInstanceKey}
  </update>
</mapper>
