version: 2
snapshot:
  widths:
    - 1280
  min-height: 1024
  percy-css: |
    *, *::before, *::after {
      animation-duration: 0s !important;
      transition-duration: 0s !important;
    }
discovery:
  network-idle-timeout: 500
