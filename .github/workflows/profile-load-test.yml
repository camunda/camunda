# description: Profiles a running load test cluster using async-profiler.
#              When no pod is specified, profiles all 3 pods in parallel, each running all
#              profile types (cpu, wall, alloc events) -- 9 parallel jobs total.
#              When a specific pod is given, profiles that pod with cpu event only.
#              Uploads flamegraph artifacts named
#              flamegraph-{test-type}-{database}-{event}-{pod}-{date} (the test-type segment
#              is omitted when the test-type input is empty).
# called-by: camunda-pr-load-test.yml, camunda-daily-load-tests.yml
# type: CI
# owner: @camunda/zeebe-distributed-platform
name: Profile running load test
on:
  workflow_dispatch:
    inputs:
      name:
        description: 'Specifies the name of the load test to profile'
        required: true
      pod:
        description: 'Specifies the pod name to profile. Leave empty to profile all three pods (camunda-0, camunda-1, camunda-2) with different events, or specify a pod name for CPU profiling only.'
        default: ''
        required: false
      profiler_options:
        description: 'Optional additional flags for async-profiler (e.g., "-t" for flamegraph format)'
        default: ''
        required: false
      database:
        description: 'Database variant'
        required: true
        type: string
      test-type:
        description: 'Optional short label identifying the calling test variant (e.g. "grpc", "rest"), used to disambiguate artifact/file names when multiple variants run in the same workflow. Leave empty to omit it.'
        default: ''
        required: false
      duration-seconds:
        description: 'Profiling duration in seconds'
        default: '100'
        required: false
  workflow_call:
    inputs:
      name:
        description: 'Specifies the name of the load test to profile'
        required: true
        type: string
      pod:
        description: 'Specifies the pod name to profile. Leave empty to profile all three pods with different events, or specify a pod name for CPU profiling only.'
        default: ''
        required: false
        type: string
      profiler_options:
        description: 'Optional additional flags for async-profiler (e.g., "-t" for flamegraph format)'
        default: ''
        required: false
        type: string
      database:
        description: 'Database variant'
        required: true
        type: string
      test-type:
        description: 'Optional short label identifying the calling test variant (e.g. "grpc", "rest"), used to disambiguate artifact/file names when multiple variants run in the same workflow. Leave empty to omit it.'
        default: ''
        required: false
        type: string
      duration-seconds:
        description: 'Profiling duration in seconds'
        default: '100'
        required: false
        type: string

defaults:
  run:
    # use bash shell by default to ensure pipefail behavior is the default
    # see https://docs.github.com/en/actions/using-workflows/workflow-syntax-for-github-actions#exit-codes-and-error-action-preference
    shell: bash

env:
  CLUSTER: camunda-benchmark-prod
  TELEPORT_JOIN_TOKEN: infra-benchmark-prod-github-action-zeebe

  DATABASE: ${{ inputs.database }}
  NAME: ${{ inputs.name }}
  POD: ${{ inputs.pod }}
  PROFILER_OPTIONS: ${{ inputs.profiler_options }}
  PROFILING_DURATION: ${{ inputs.duration-seconds }}
  TEST_TYPE: ${{ inputs.test-type }}

jobs:
  profile-all-pods:
    name: Profile ${{ matrix.event }} on ${{ matrix.pod }}
    runs-on: ubuntu-latest
    # INVARIANT: timeout-minutes * 60 must stay > duration-seconds (plus a buffer for the
    # checkout/teleport-auth/kubectl-context/upload-artifact/summary steps), otherwise the
    # job may time out mid-profile. GitHub Actions does not support arithmetic in
    # `timeout-minutes` expressions (no +/-/*// operators), so this can't be computed from
    # `inputs.duration-seconds` automatically -- bump it manually if a caller raises
    # duration-seconds. Largest known caller today: camunda-daily-load-tests.yml's
    # profile-grpc/profile-rest jobs pass duration-seconds: '1800' (30 min).
    timeout-minutes: 60
    if: ${{ inputs.pod == '' }}
    permissions:
      contents: 'read'
      id-token: 'write'
    strategy:
      fail-fast: false
      matrix:
        pod: [camunda-0, camunda-1, camunda-2]
        event: [cpu, wall, alloc]
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false
      - name: Set up Teleport
        uses: teleport-actions/setup@b638ff596557cc3959eb6b5287d5e58e0c8ac6a6 # v1.1.1
        with:
          proxy: camunda.teleport.sh:443
          version: auto

      - name: Authenticate to Cluster using Teleport
        uses: teleport-actions/auth-k8s@0f46164469ae4fcd4d359d40e06bab17d4be17c9 # v2.0.6
        with:
          proxy: camunda.teleport.sh:443
          token: ${{ env.TELEPORT_JOIN_TOKEN }}
          kubernetes-cluster: ${{ env.CLUSTER }}

      - name: Set right namespace
        run: |
          kubectl config set-context --current --namespace="${NAME}"
      - name: Compute run date
        id: run-date
        run: echo "date=$(date +%Y%m%d)" >> "$GITHUB_OUTPUT"
      - name: Execute profiling
        env:
          POD: ${{ matrix.pod }}
          EVENT: ${{ matrix.event }}
        run: >
          ./load-tests/docs/scripts/executeProfiling.sh "${POD}" "${EVENT}" "${DATABASE}" "${PROFILER_OPTIONS}" "${TEST_TYPE}"
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: flamegraph${{ inputs.test-type != '' && format('-{0}', inputs.test-type) || '' }}-${{ inputs.database }}-${{ matrix.event }}-${{ matrix.pod }}-${{ steps.run-date.outputs.date }}
          path: ${{ matrix.pod }}*
      - name: Summarize profiling
        if: success()
        run: |
          cat >> "$GITHUB_STEP_SUMMARY" <<EOF
            ## Profiling \`${NAME}\` - ${EVENT} on ${POD}
            Async profiling (${EVENT}) has been executed on ${NAME}-${POD}. Please download the artifact.
          EOF
        env:
          POD: ${{ matrix.pod }}
          EVENT: ${{ matrix.event }}
      - name: Observe build status
        if: always()
        continue-on-error: true
        uses: ./.github/actions/observe-build-status
        with:
          job_name: "Profile ${{ matrix.event }} on ${{ matrix.pod }}"
          build_status: ${{ job.status }}
          secret_vault_address: ${{ secrets.VAULT_ADDR }}
          secret_vault_roleId: ${{ secrets.VAULT_ROLE_ID }}
          secret_vault_secretId: ${{ secrets.VAULT_SECRET_ID }}

  profile-single-pod:
    name: Profile CPU on single pod ${{ inputs.pod }}
    runs-on: ubuntu-latest
    # INVARIANT: timeout-minutes * 60 must stay > duration-seconds (plus a buffer for the
    # checkout/teleport-auth/kubectl-context/upload-artifact/summary steps), otherwise the
    # job may time out mid-profile. GitHub Actions does not support arithmetic in
    # `timeout-minutes` expressions (no +/-/*// operators), so this can't be computed from
    # `inputs.duration-seconds` automatically -- bump it manually if a caller raises
    # duration-seconds. Largest known caller today: camunda-daily-load-tests.yml's
    # profile-grpc/profile-rest jobs pass duration-seconds: '1800' (30 min).
    timeout-minutes: 60
    if: ${{ inputs.pod != '' }}
    permissions:
      contents: 'read'
      id-token: 'write'
    steps:
      - uses: actions/checkout@9c091bb21b7c1c1d1991bb908d89e4e9dddfe3e0 # v7.0.0
        with:
          persist-credentials: false
      - name: Set up Teleport
        uses: teleport-actions/setup@b638ff596557cc3959eb6b5287d5e58e0c8ac6a6 # v1.1.1
        with:
          proxy: camunda.teleport.sh:443
          version: auto

      - name: Authenticate to Cluster using Teleport
        uses: teleport-actions/auth-k8s@0f46164469ae4fcd4d359d40e06bab17d4be17c9 # v2.0.6
        with:
          proxy: camunda.teleport.sh:443
          token: ${{ env.TELEPORT_JOIN_TOKEN }}
          kubernetes-cluster: ${{ env.CLUSTER }}
      - name: Set right namespace
        run: |
          kubectl config set-context --current --namespace="${NAME}"
      - name: Compute run date
        id: run-date
        run: echo "date=$(date +%Y%m%d)" >> "$GITHUB_OUTPUT"
      - name: Execute profiling
        run: >
          ./load-tests/docs/scripts/executeProfiling.sh "${POD}" cpu "${DATABASE}" "${PROFILER_OPTIONS}" "${TEST_TYPE}"
      - uses: actions/upload-artifact@043fb46d1a93c77aae656e7c1c64a875d1fc6a0a # v7.0.1
        with:
          name: flamegraph${{ inputs.test-type != '' && format('-{0}', inputs.test-type) || '' }}-${{ inputs.database }}-cpu-${{ inputs.pod }}-${{ steps.run-date.outputs.date }}
          path: ${{ inputs.pod }}*
      - name: Summarize profiling
        if: success()
        run: |
          cat >> "$GITHUB_STEP_SUMMARY" <<EOF
            ## Profiling \`${NAME}\` - CPU on ${POD}
            Async profiling (CPU) has been executed on ${NAME}-${POD}. Please download the artifact.
          EOF
      - name: Observe build status
        if: always()
        continue-on-error: true
        uses: ./.github/actions/observe-build-status
        with:
          build_status: ${{ job.status }}
          secret_vault_address: ${{ secrets.VAULT_ADDR }}
          secret_vault_roleId: ${{ secrets.VAULT_ROLE_ID }}
          secret_vault_secretId: ${{ secrets.VAULT_SECRET_ID }}
