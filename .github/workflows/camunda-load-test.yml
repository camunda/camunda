# description: This workflow deploys a load test with a camunda cluster under test.
# We use this workflow to start ad-hoc load tests or weekly medic load tests.
# The health of the cluster and results of the load tests are monitored by Core team's regularly.
# https://dashboard.benchmark.camunda.cloud/
# called by: camunda-weekly-load-tests.yml, camunda-pr-load-test.yaml
# type: CI
# owner: camunda/orchestration-cluster-team
#
# Infos from Infra:
# - Using the infra benchmark prod cluster requires to use Teleport to authenticate.
# - Camunda images are pushed to registry.camunda.cloud/team-zeebe and pulled from there.
#   To use them in a namespace, the namespace must be labeled with "registry=harbor"
#   You must also set the image pull secret to "harbor-registry" in the helm values.

name: Camunda load test
on:
  workflow_dispatch:
    inputs:
      ref:
        description: 'Specifies the ref (e.g. branch name, commit sha or tag) from which the docker image is built and used to run the load test against'
        default: 'stable/8.8'
        type: string
        required: false
      name:
        description: 'Specifies the name of the load test'
        required: true
      ttl:
        description: 'Specifies after how many days the load test namespace should be deleted'
        type: number
        default: 1
        required: false
      reuse-tag:
        description: 'Docker image tag, that should be reused for the load test. Allows to skip the docker image build step'
        type: string
        default: ""
        required: false
      use-official-docker-images:
        description: 'Use official Camunda Docker images from Docker Hub instead of building from source'
        type: boolean
        required: false
        default: false
      scenario:
        description: 'Choose the variant of workload to use for the load test. When not set to "custom", this will override the load-test-load input.'
        required: false
        type: choice
        options:
          - custom
          - latency
          - realistic
          - realisticLargePayload
          - multiInstanceElements
          - typical
          - max
        default: custom
      load-test-load:
        description: 'Specifies which load test components to deploy. For example, `starter` can be assigned with the rate at which they start instances. Allows arbitrary helm arguments, like --set starter.rate=100. This is ignored when scenario is not "custom".'
        required: false
      platform-helm-values:
        description: 'Allows to specify additional values/configurations for the Camunda platform Helm chart, which will be deployed. For example, the Orchestration cluster `resources` can be modified. Allows arbitrary helm arguments, like --set orchestration.resources.limits.memory=2Gi'
        type: string
        default: ""
        required: false
      perform-read-benchmarks:
        description: 'Perform continuous read benchmarks on secondary storage. This may influence the exporter performance as it puts more load on the secondary database. The metrics are available in the Data-Layer dashboard.'
        type: boolean
        required: false
        default: false
      stable-vms:
        description: 'Deploy to non-spot VMs'
        type: boolean
        required: false
        default: false
      enable-optimize:
        description: 'Enable Optimize for load testing'
        type: boolean
        required: false
        default: false
      build-frontend:
        description: 'Build the frontend as part of the load test image build'
        type: boolean
        required: false
        default: false
      secondary-storage-type:
        description: 'Specifies the type of the secondary database used by Camunda Platform, can be "elasticsearch", "opensearch" or "none"'
        type: choice
        options:
        - elasticsearch
        - opensearch
        - none
        default: elasticsearch
  workflow_call:
    inputs:
      ref:
        description: 'Specifies the ref (e.g. branch name, commit sha or tag) from which the docker image is built and used to run the load test against'
        default: 'stable/8.8'
        type: string
        required: false
      name:
        description: 'Specifies the name of the load test'
        type: string
        required: true
      ttl:
        description: 'Specifies after how many days the load test namespace should be deleted'
        type: number
        default: 1
        required: false
      reuse-tag:
        description: 'Docker image tag, that should be reused for the load test. Allows to skip the docker image build step'
        type: string
        default: ""
        required: false
      use-official-docker-images:
        description: 'Use official Camunda Docker images from Docker Hub instead of building from source'
        type: boolean
        required: false
        default: false
      scenario:
        description: 'Choose the variant of workload to use for the load test. Options: custom, latency, realistic, realisticLargePayload, typical, max. If specified, this will override the load-test-load input when not set to "custom".'
        type: string
        required: false
        default: custom
      load-test-load:
        description: 'Specifies which load test components to deploy. For example, `starter` can be assigned with the rate at which they start instances. Allows arbitrary helm arguments, like --set starter.rate=100. This is ignored when scenario is not "custom".'
        type: string
        required: false
      platform-helm-values:
        description: 'Allows to specify additional values/configurations for the Camunda platform Helm chart, which will be deployed. For example, the Orchestration cluster `resources` can be modified. Allows arbitrary helm arguments, like --set orchestration.resources.limits.memory=2Gi'
        type: string
        default: ""
        required: false
      publish:
        description: 'Where to publish the results, can be "slack" or "comment"'
        default: ""
        type: string
        required: false
      perform-read-benchmarks:
        description: 'Perform continuous read benchmarks on secondary storage. This may influence the exporter performance as it puts more load on the secondary database. The metrics are available in the Data-Layer dashboard.'
        type: boolean
        required: false
        default: false
      stable-vms:
        description: 'Deploy to non-spot VMs'
        type: boolean
        required: false
        default: false
      enable-optimize:
        description: 'Enable Optimize for load testing'
        type: boolean
        required: false
        default: false
      build-frontend:
        description: 'Build the frontend as part of the load test image build'
        type: boolean
        required: false
        default: false
      secondary-storage-type:
        description: 'Specifies the type of the secondary database used by Camunda Platform, can be "elasticsearch", "opensearch" or "none"'
        # type choice is invalid here
        type: string
        required: false
        default: elasticsearch

env:
  CLUSTER: camunda-benchmark-prod  # change this to "camunda-benchmark-dev" when experimenting
  TELEPORT_JOIN_TOKEN: infra-benchmark-prod-github-action-zeebe  # change this to "infra-benchmark-dev-github-action-zeebe" when experimenting
  HELM_CHART: camunda-platform-8.8
  # Namespaces MUST start with c8 due to access policy
  NAMESPACE: ${{ startsWith(inputs.name, 'c8-') && inputs.name || format('c8-{0}', inputs.name) }}
  IMAGE_REPOSITORY: registry.camunda.cloud/team-zeebe

defaults:
  run:
    # use bash shell by default to ensure pipefail behavior is the default
    # see https://docs.github.com/en/actions/using-workflows/workflow-syntax-for-github-actions#exit-codes-and-error-action-preference
    shell: bash

jobs:
  calculate-image-tag:
    name: Calculate Image Tag
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions: { }
    outputs:
      image-tag: ${{ inputs.reuse-tag != '' && inputs.reuse-tag || steps.image-tag.outputs.image-tag }}
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ inputs.ref }}
      - name: Get image tag
        id: image-tag
        run: |
          echo "image-tag=${{ inputs.name }}-$(git rev-parse --short HEAD)" >> "$GITHUB_OUTPUT"
      - name: Validate Docker image tags exist
        if: ${{ inputs.use-official-docker-images == true && inputs.reuse-tag != '' }}
        env:
          TAG: ${{ inputs.reuse-tag }}
          ENABLE_OPTIMIZE: ${{ inputs.enable-optimize }}
        run: |
          validate_image() {
            local image="$1"
            local max_retries=3
            local retry_delay=5
            local attempt=1

            while true; do
              echo "Validating Docker image ${image} (attempt ${attempt}/${max_retries})"
              manifest_output=$(docker manifest inspect "${image}" 2>&1 >/dev/null)
              status=$?

              if [ "${status}" -eq 0 ]; then
                echo "Docker image ${image} exists"
                return 0
              fi

              # Distinguish missing tag from transient errors
              if echo "${manifest_output}" | grep -qiE "manifest unknown|no such manifest|not found"; then
                echo "::error::Docker image ${image} does not exist on Docker Hub"
                return 1
              fi

              if [ "${attempt}" -ge "${max_retries}" ]; then
                echo "::error::Failed to validate Docker image ${image} after ${max_retries} attempts: ${manifest_output}"
                return "${status}"
              fi

              echo "Transient error (attempt ${attempt}/${max_retries}), retrying in ${retry_delay}s..."
              sleep "${retry_delay}"
              attempt=$((attempt + 1))
            done
          }

          validate_image "camunda/camunda:${TAG}" || exit 1

          if [[ "${ENABLE_OPTIMIZE}" == "true" ]]; then
            validate_image "camunda/optimize:${TAG}" || exit 1
          fi

  calculate-scenario-config:
    name: Calculate Scenario Configuration
    runs-on: ubuntu-latest
    timeout-minutes: 5
    permissions: { }
    outputs:
      load-test-load: ${{ steps.config.outputs.load-test-load }}
      platform-additional-config: ${{ steps.config.outputs.platform-additional-config }}
    steps:
      - name: Calculate configuration based on scenario
        id: config
        run: |
          scenario="${{ inputs.scenario }}"

          # Default to custom scenario if not provided
          if [[ -z "$scenario" ]]; then
            scenario="custom"
          fi

          case "$scenario" in
            latency)
              echo "load-test-load=--set starter.rate=1 --set workers.worker.replicas=1" >> "$GITHUB_OUTPUT"
              echo "platform-additional-config=" >> "$GITHUB_OUTPUT"
              ;;
            realistic)
              echo "load-test-load=-f https://raw.githubusercontent.com/camunda/camunda-load-tests-helm/refs/heads/main/charts/camunda-load-tests/values-realistic-benchmark.yaml" >> "$GITHUB_OUTPUT"
              echo "platform-additional-config=" >> "$GITHUB_OUTPUT"
              ;;
            realisticLargePayload)
              echo "load-test-load=-f https://raw.githubusercontent.com/camunda/camunda-load-tests-helm/refs/heads/main/charts/camunda-load-tests/values-realistic-benchmark.yaml --set starter.payloadPath=bpmn/realistic/largeRealisticPayload.json --set starter.rate=1 --set global.extraEnvVars[1].name=CONFIG_FORCE_app_starter_rateDuration --set global.extraEnvVars[1].value=5m --set global.extraEnvVars[2].name=CONFIG_FORCE_app_worker_fetchVariables_0 --set global.extraEnvVars[2].value=_ --set workers.customer-notification.payloadPath=bpmn/typical_payload.json --set workers.extract-data-from-document.payloadPath=bpmn/typical_payload.json --set workers.dispute-process-request-proof-from-vendor.payloadPath=bpmn/typical_payload.json --set workers.dispute-process-request-get-vendor-info.payloadPath=bpmn/typical_payload.json --set workers.refunding.payloadPath=bpmn/typical_payload.json --set workers.inform-about-successful-claim.payloadPath=bpmn/typical_payload.json" >> "$GITHUB_OUTPUT"
              echo "platform-additional-config=" >> "$GITHUB_OUTPUT"
              ;;
            multiInstanceElements)
              echo "load-test-load=--set starter.rate=1 --set global.extraEnvVars[1].name=CONFIG_FORCE_app_starter_rateDuration --set global.extraEnvVars[1].value=5m --set starter.processId=multiInstanceElements --set starter.bpmnXmlPath=bpmn/multiInstanceElements.bpmn --set starter.payloadPath=bpmn/multiInstanceElementsPayload.json --set workers.worker.replicas=0" >> "$GITHUB_OUTPUT"
              echo "platform-additional-config=" >> "$GITHUB_OUTPUT"
              ;;
            typical)
              # Output typical configuration directly to avoid quote escaping issues
              echo "load-test-load=--set starter.rate=50 --set workers.worker.replicas=6 --set starter.bpmnXmlPath=bpmn/typical_process.bpmn" >> "$GITHUB_OUTPUT"
              echo "platform-additional-config=" >> "$GITHUB_OUTPUT"
              ;;
            max)
              echo "load-test-load=--set starter.rate=300" >> "$GITHUB_OUTPUT"
              # For the max stress scenario, disable the experimental consistency checks to avoid overhead.
              # We inject the overrides via --set-file into extraConfiguration (application-override.yml),
              # which is loaded after (and thus takes priority over) the base application.yml.
              # All values files define extraConfiguration.application-override.yml (empty by default),
              # so this override works regardless of which secondary storage type is selected.
              # The 8.8 Helm chart expects extraConfiguration as a map (key=filename, value=content),
              # so we use the map key syntax with escaped dots instead of array indexing.
              # Path is relative to the helm working dir: load-tests/setup/$NAMESPACE/
              echo "platform-additional-config=--set-file 'orchestration.extraConfiguration.application-override\.yml=./camunda-platform-override-values.yaml'" >> "$GITHUB_OUTPUT"
              ;;
            custom)
              # Use the provided load-test-load inputs
              echo "load-test-load=${{ inputs.load-test-load }}" >> "$GITHUB_OUTPUT"
              echo "platform-additional-config=" >> "$GITHUB_OUTPUT"
              ;;
            *)
              echo "Error: Unknown scenario '$scenario'"
              exit 1
              ;;
          esac
      - name: Observe build status
        if: always()
        continue-on-error: true
        uses: ./.github/actions/observe-build-status
        with:
          build_status: ${{ job.status }}
          secret_vault_address: ${{ secrets.VAULT_ADDR }}
          secret_vault_roleId: ${{ secrets.VAULT_ROLE_ID }}
          secret_vault_secretId: ${{ secrets.VAULT_SECRET_ID }}

  build-camunda-image:
    name: Build Camunda
    needs: calculate-image-tag
    if: ${{ inputs.reuse-tag == '' }}
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      contents: 'read'
      id-token: 'write'
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ inputs.ref }}
      - uses: ./.github/actions/setup-build
        with:
          dockerhub-readonly: true
          harbor: true
          vault-address: ${{ secrets.VAULT_ADDR }}
          vault-role-id: ${{ secrets.VAULT_ROLE_ID }}
          vault-secret-id: ${{ secrets.VAULT_SECRET_ID }}
          minimus: true
      - uses: ./.github/actions/build-frontend
        name: Build Operate Frontend
        id: build-operate-fe
        if: ${{ inputs.build-frontend }}
        with:
          directory: ./operate/client
          package-manager: "npm"
      - uses: ./.github/actions/build-frontend
        name: Build Tasklist Frontend
        id: build-tasklist-fe
        if: ${{ inputs.build-frontend }}
        with:
          directory: ./tasklist/client
          package-manager: "npm"
      - uses: ./.github/actions/build-zeebe
        name: Build Camunda Platform
        id: build-camunda
        with:
          maven-extra-args: -PskipFrontendBuild ${{ inputs.enable-optimize && '-Pinclude-optimize' || '-P\!include-optimize' }}
      - uses: ./.github/actions/build-platform-docker
        name: Build Camunda Docker Image
        with:
          repository: '${{ env.IMAGE_REPOSITORY }}/camunda'
          revision: ${{ inputs.ref }}
          push: true
          version: ${{ needs.calculate-image-tag.outputs.image-tag }}
          distball: ${{ steps.build-camunda.outputs.distball }}
          dockerfile: 'camunda.Dockerfile'
      - uses: ./.github/actions/build-platform-docker
        name: Build Optimize Docker Image
        if: ${{ inputs.enable-optimize }}
        with:
          repository: '${{ env.IMAGE_REPOSITORY }}/optimize'
          revision: ${{ inputs.ref }}
          push: true
          version: ${{ needs.calculate-image-tag.outputs.image-tag }}
          distball: 'optimize-distro/target/camunda-optimize-*.tar.gz'
          dockerfile: 'optimize.Dockerfile'
      - name: Observe build status
        if: always()
        continue-on-error: true
        uses: ./.github/actions/observe-build-status
        with:
          build_status: ${{ job.status }}
          secret_vault_address: ${{ secrets.VAULT_ADDR }}
          secret_vault_roleId: ${{ secrets.VAULT_ROLE_ID }}
          secret_vault_secretId: ${{ secrets.VAULT_SECRET_ID }}

  build-load-test-images:
    name: Build Starter and Worker
    if: ${{ inputs.reuse-tag == '' || inputs.use-official-docker-images == true }}
    runs-on: ubuntu-latest
    timeout-minutes: 30
    needs: calculate-image-tag
    permissions:
      contents: 'read'
      id-token: 'write'
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ inputs.ref }}
      - uses: ./.github/actions/setup-build
        with:
          dockerhub-readonly: true
          harbor: true
          vault-address: ${{ secrets.VAULT_ADDR }}
          vault-role-id: ${{ secrets.VAULT_ROLE_ID }}
          vault-secret-id: ${{ secrets.VAULT_SECRET_ID }}
      - run: ./mvnw -B -D skipTests -D skipChecks -P\!include-optimize -pl zeebe/benchmarks/project -am install
      - name: Build Starter Image
        run: ./mvnw -pl zeebe/benchmarks/project jib:build -P starter -P\!include-optimize -D image="${{ env.IMAGE_REPOSITORY }}/starter:${{ needs.calculate-image-tag.outputs.image-tag }}"
      - name: Build Worker Image
        run: ./mvnw -pl zeebe/benchmarks/project jib:build -P worker -P\!include-optimize -D image="${{ env.IMAGE_REPOSITORY }}/worker:${{ needs.calculate-image-tag.outputs.image-tag }}"
      - name: Observe build status
        if: always()
        continue-on-error: true
        uses: ./.github/actions/observe-build-status
        with:
          build_status: ${{ job.status }}
          secret_vault_address: ${{ secrets.VAULT_ADDR }}
          secret_vault_roleId: ${{ secrets.VAULT_ROLE_ID }}
          secret_vault_secretId: ${{ secrets.VAULT_SECRET_ID }}

  deploy-cluster-under-test:
    name: Deploy cluster under test
    needs:
      - calculate-image-tag
      - calculate-scenario-config
      - build-camunda-image
      - build-load-test-images
    # To have the possibility to re-deploy the tests without rebuilding the images,
    # the deploy will be run, when build is skipped or successful.
    if: |
      always() &&
      needs.calculate-image-tag.result == 'success' &&
      (needs.build-camunda-image.result == 'success' || needs.build-camunda-image.result == 'skipped') &&
      (needs.build-load-test-images.result == 'success' || needs.build-load-test-images.result == 'skipped')
    runs-on: ubuntu-latest
    timeout-minutes: 30
    permissions:
      contents: 'read'
      id-token: 'write'
    steps:
      - uses: actions/checkout@v6
        with:
          ref: ${{ inputs.ref }}
      # Authenticate with Teleport
      - name: Set up Teleport
        uses: teleport-actions/setup@v1
        with:
            version: 17.2.2

      - name: Authenticate to Cluster using Teleport
        uses: teleport-actions/auth-k8s@v2
        with:
            proxy: camunda.teleport.sh:443
            token: ${{ env.TELEPORT_JOIN_TOKEN }}
            kubernetes-cluster: ${{ env.CLUSTER }}

      - name: Create namespace if not exists
        run: |
          cd zeebe/benchmarks/setup
          # The namespace label created-by will not be able resolve the actual user which triggered
          # the GitHub action by default. We need to correctly configure the git user here to make it work
          git config user.name ${{ github.actor }}
          # Create the namespace for the load test
          ./newBenchmark.sh ${{ env.NAMESPACE }} ${{ inputs.secondary-storage-type }} ${{ inputs.ttl }} ${{ inputs.enable-optimize }}
          # Annotate namespace with the workflow run URL for traceability
          kubectl annotate namespace ${{ env.NAMESPACE }} \
            "github.com/workflow-run-url=${{ github.server_url }}/${{ github.repository }}/actions/runs/${{ github.run_id }}" \
            --overwrite
      - name: Install latest Camunda Platform
        run: |
          cd zeebe/benchmarks/setup/${{ env.NAMESPACE }}

          # Build load test configuration
          load_test_config="--set global.image.tag=${{ needs.calculate-image-tag.outputs.image-tag }} \
                            --set global.image.repository=${{ env.IMAGE_REPOSITORY }} \
                            --set global.image.pullSecrets[0].name=harbor-registry \
                            --set global.extraEnvVars[0].name=CONFIG_FORCE_app_performReadBenchmarks \
                            --set global.extraEnvVars[0].value=${{inputs.perform-read-benchmarks}}"
          # Append load-test-load configuration if not empty
          if [[ -n "${{ needs.calculate-scenario-config.outputs.load-test-load }}" ]]; then
            load_test_config="$load_test_config ${{ needs.calculate-scenario-config.outputs.load-test-load }}"
          fi

          # Resolve image registry/repository based on whether official images are used
          image_registry="${{ inputs.use-official-docker-images && 'docker.io' || 'registry.camunda.cloud' }}"
          camunda_repo="${{ inputs.use-official-docker-images && 'camunda/camunda' || 'team-zeebe/camunda' }}"
          optimize_repo="${{ inputs.use-official-docker-images && 'camunda/optimize' || 'team-zeebe/optimize' }}"
          image_tag="${{ needs.calculate-image-tag.outputs.image-tag }}"

          # Build platform configuration
          additional_platform_config="--set global.image.tag=${image_tag} \
            --set orchestration.image.registry=${image_registry} \
            --set orchestration.image.repository=${camunda_repo} \
            --set orchestration.image.tag=${image_tag}"

          # Append optimize image config if enabled
          if [[ "${{ inputs.enable-optimize }}" == "true" ]]; then
            additional_platform_config+=" \
              --set optimize.image.registry=${image_registry} \
              --set optimize.image.repository=${optimize_repo} \
              --set optimize.image.tag=${image_tag}"
          fi

          # Append user-provided helm values if present
          if [[ -n "${{ inputs.platform-helm-values }}" ]]; then
            additional_platform_config+=" ${{ inputs.platform-helm-values }}"
          fi

          # Append scenario-specific platform configuration if present (e.g. max scenario disables consistency checks)
          if [[ -n "${{ needs.calculate-scenario-config.outputs.platform-additional-config }}" ]]; then
            additional_platform_config+=" ${{ needs.calculate-scenario-config.outputs.platform-additional-config }}"
          fi

          make ${{ inputs.stable-vms && 'install-stable' || 'install' }} \
            secondary_storage=${{ inputs.secondary-storage-type }} \
            enable_optimize=${{ inputs.enable-optimize }} \
            additional_platform_configuration="$additional_platform_config" \
            additional_load_test_configuration="$load_test_config"
      - name: Summarize platform deployment
        if: success()
        run: |
          cat >> "$GITHUB_STEP_SUMMARY" <<EOF
            ## Camunda platform \`${{ inputs.name }}\` values
            \`\`\`yaml
            $(helm get values ${{ env.NAMESPACE }} -n ${{ env.NAMESPACE }})
            \`\`\`
          EOF
      - name: Summarize load test deployment
        if: success()
        run: |
          cat >> "$GITHUB_STEP_SUMMARY" <<EOF
            # Load test
            The test has been set up successfully. You can observe it [here](https://dashboard.benchmark.camunda.cloud/d/zeebe-dashboard/zeebe?var-namespace=${{ env.NAMESPACE }})
            ## Load test \`${{ inputs.name }}\` values
            \`\`\`yaml
            $(helm get values ${{ env.NAMESPACE }}-test -n ${{ env.NAMESPACE }})
            \`\`\`
          EOF
      - name: Observe build status
        if: always()
        continue-on-error: true
        uses: ./.github/actions/observe-build-status
        with:
          build_status: ${{ job.status }}
          secret_vault_address: ${{ secrets.VAULT_ADDR }}
          secret_vault_roleId: ${{ secrets.VAULT_ROLE_ID }}
          secret_vault_secretId: ${{ secrets.VAULT_SECRET_ID }}
