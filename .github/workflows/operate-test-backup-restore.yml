name: Operate Run Test Backup/Restore
on:
  schedule:
    - cron: "0 5 * * *"
jobs:
  run-backup-restore-tests:
    uses: ./.github/workflows/operate-run-tests.yml
    with:
      command: mvn -B -pl operate/qa/backup-restore-tests -P -docker,-skipTests verify
    secrets: inherit
