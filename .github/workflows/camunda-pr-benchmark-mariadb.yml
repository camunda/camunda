# type: CI
# owner: @camunda/reliability-testing
name: Camunda PR Benchmark (mariadb)
on:
  pull_request:
    types:
      - labeled
      - unlabeled
      - synchronize
      - closed

defaults:
  run:
    shell: bash

jobs:
  benchmark:
    name: Benchmark (mariadb)
    if: >
      (contains(fromJSON('["labeled", "unlabeled"]'), github.event.action) && github.event.label.name == 'benchmark-mariadb') ||
      (contains(fromJSON('["synchronize", "closed"]'), github.event.action) && contains(github.event.pull_request.labels.*.name, 'benchmark-mariadb'))
    uses: ./.github/workflows/pr-benchmark-pipeline.yml
    secrets: inherit
    with:
      label: 'benchmark-mariadb'
      secondary-storage-type: 'mariadb'
      ref: ${{ github.event.pull_request.head.ref }}
