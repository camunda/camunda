# description: Runs a weekly check to see if Renovate configuration migration is needed and posts it to Slack
# type: CI Helper
# owner: @camunda/engineering-operations

---
name: Renovate Weekly

on:
  schedule:
    - cron: '0 6 * * 1'  # Monday
  pull_request:
    paths:
      - '.github/workflows/renovate-weekly.yml'
  workflow_dispatch: {}

env:
  TEST_OWNER: '@camunda/engineering-operations'
  GHA_BEST_PRACTICES_LINTER: enabled

jobs:
  config-migration-needed:
    runs-on: ubuntu-latest
    # 8 min accommodates the 3-attempt retry below (~6.5 min worst case).
    timeout-minutes: 8
    permissions: {}  # GITHUB_TOKEN unused in this job
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3
      - uses: actions/setup-node@53b83947a5a98c8d113130e565377fae1a50d02f # v6.3.0
        with:
          node-version: 24
      # Retry to absorb transient npm-registry resolution failures when npx resolves
      # renovate@latest and its transitive deps on a fresh run (see INC-6869).
      - name: Validate Renovate config
        uses: nick-fields/retry@ad984534de44a9489a53aefd81eb77f87c70dc60 # v4.0.0
        with:
          max_attempts: 3
          retry_on: error
          retry_wait_seconds: 15
          # 2 min/attempt keeps all 3 attempts (~6.5 min worst case) within the
          # job timeout; validation normally completes in well under a minute.
          timeout_minutes: 2
          command: npx --yes --package "renovate@latest" -c "renovate-config-validator --strict"
      - name: Observe build status
        if: always()
        continue-on-error: true
        uses: ./.github/actions/observe-build-status
        with:
          build_status: ${{ job.status }}
          secret_vault_address: ${{ secrets.VAULT_ADDR }}
          secret_vault_roleId: ${{ secrets.VAULT_ROLE_ID }}
          secret_vault_secretId: ${{ secrets.VAULT_SECRET_ID }}

  notify:
    name: Send Slack notification
    runs-on: ubuntu-latest
    needs: [config-migration-needed]
    if: ${{ always() && needs.config-migration-needed.result != 'success' && github.event_name == 'schedule' }}
    timeout-minutes: 5
    permissions: {}  # GITHUB_TOKEN unused in this job
    steps:
      - uses: actions/checkout@df4cb1c069e1874edd31b4311f1884172cec0e10 # v6.0.3

      - name: Import Secrets
        id: secrets
        uses: hashicorp/vault-action@4c06c5ccf5c0761b6029f56cfb1dcf5565918a3b # v3.4.0
        with:
          url: ${{ secrets.VAULT_ADDR }}
          method: approle
          roleId: ${{ secrets.VAULT_ROLE_ID }}
          secretId: ${{ secrets.VAULT_SECRET_ID }}
          exportEnv: false # we rely on step outputs, no need for environment variables
          secrets: |
            secret/data/products/camunda/ci/github-actions SLACK_TOPMONOREPOCI_WEBHOOK_URL;

      - name: Send failure Slack notification
        uses: slackapi/slack-github-action@91efab103c0de0a537f72a35f6b8cda0ee76bf0a # v2.1.1
        with:
          webhook: ${{ steps.secrets.outputs.SLACK_TOPMONOREPOCI_WEBHOOK_URL }}
          webhook-type: incoming-webhook
          # For posting a rich message using Block Kit
          payload: |
            {
              "blocks": [
                {
                  "type": "section",
                  "text": {
                    "type": "mrkdwn",
                    "text": ":alarm: *Renovate Config Migration* needed on `main`!\n"
                  }
                },
                {
                  "type": "section",
                  "text": {
                    "type": "mrkdwn",
                    "text": "Please check this <https://github.com/camunda/camunda/actions/runs/${{ github.run_id }}|GHA workflow run> for details, verify that the version of Hosted Renovate we're using actually supports the new config and create a ticket."
                  }
                }
              ]
            }

      - name: Observe build status
        if: always()
        continue-on-error: true
        uses: ./.github/actions/observe-build-status
        with:
          build_status: ${{ job.status }}
          secret_vault_address: ${{ secrets.VAULT_ADDR }}
          secret_vault_roleId: ${{ secrets.VAULT_ROLE_ID }}
          secret_vault_secretId: ${{ secrets.VAULT_SECRET_ID }}
