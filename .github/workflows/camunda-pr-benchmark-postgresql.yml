# type: CI
# owner: @camunda/reliability-testing
name: Camunda PR Benchmark (postgresql)
on:
  pull_request:
    types:
      - labeled
      - unlabeled
      - synchronize
      - closed

defaults:
  run:
    shell: bash

jobs:
  benchmark:
    name: Benchmark (postgresql)
    if: >
      (contains(fromJSON('["labeled", "unlabeled"]'), github.event.action) && github.event.label.name == 'benchmark-postgresql') ||
      (contains(fromJSON('["synchronize", "closed"]'), github.event.action) && contains(github.event.pull_request.labels.*.name, 'benchmark-postgresql'))
    uses: ./.github/workflows/pr-benchmark-pipeline.yml
    secrets: inherit
    with:
      label: 'benchmark-postgresql'
      secondary-storage-type: 'postgresql'
      ref: ${{ github.event.pull_request.head.ref }}
