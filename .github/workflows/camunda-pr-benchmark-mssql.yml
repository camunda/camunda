# type: CI
# owner: @camunda/reliability-testing
name: Camunda PR Benchmark (mssql)
on:
  pull_request:
    types:
      - labeled
      - unlabeled
      - synchronize
      - closed

defaults:
  run:
    shell: bash

jobs:
  benchmark:
    name: Benchmark (mssql)
    if: >
      (contains(fromJSON('["labeled", "unlabeled"]'), github.event.action) && github.event.label.name == 'benchmark-mssql') ||
      (contains(fromJSON('["synchronize", "closed"]'), github.event.action) && contains(github.event.pull_request.labels.*.name, 'benchmark-mssql'))
    uses: ./.github/workflows/pr-benchmark-pipeline.yml
    secrets: inherit
    with:
      label: 'benchmark-mssql'
      secondary-storage-type: 'mssql'
      ref: ${{ github.event.pull_request.head.ref }}
