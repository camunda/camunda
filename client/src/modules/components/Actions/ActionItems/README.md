A single action item

```js
<ActionItems>
  <ActionItems.Item type="RETRY" onClick={() => console.log('foobar')} />
</ActionItems>
```

Multiple action items

```js
<ActionItems>
  <ActionItems.Item type="RETRY" onClick={() => console.log('foobar')} />
  <ActionItems.Item type="RETRY" onClick={() => console.log('foobar')} />
  <ActionItems.Item type="RETRY" onClick={() => console.log('foobar')} />
</ActionItems>
```
