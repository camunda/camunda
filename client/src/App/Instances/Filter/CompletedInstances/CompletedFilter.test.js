import React from 'react';
import {shallow} from 'enzyme';

import CompletedFilter from './';

describe('CompletedFilter', () => {
  let node;

  beforeEach(() => {
    // node = shallow(<CompletedFilter />);
  });

  it('should render three checkboxes with labels', () => {
    //given
    // expect(node.exists()).toBe(true);
    //when
    //then
  });

  it('should render the Checkboxes to be unticked by default', () => {
    //given
    //when
    //then
  });

  it('should set parent to checked if both children are selected', () => {
    //given
    //when
    //then
  });

  it('should set parent to indeterminate if only one child is selected', () => {
    //given
    //when
    //then
  });
});
