<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>
  <parent>
    <groupId>io.camunda</groupId>
    <artifactId>camunda-library-parent</artifactId>
    <version>8.7.36-SNAPSHOT</version>
    <relativePath>../../library-parent/pom.xml</relativePath>
  </parent>

  <artifactId>camunda-spring-boot-4-starter</artifactId>

  <name>Camunda Spring Boot 4 Starter</name>
  <description>Camunda Spring Boot Starter for Spring Boot 4.x</description>

  <licenses>
    <license>
      <name>Apache License, Version 2.0</name>
      <url>http://www.apache.org/licenses/LICENSE-2.0</url>
    </license>
  </licenses>

  <properties>
    <version.java>17</version.java>
    <license.header>com/mycila/maven/plugin/license/templates/APACHE-2.txt</license.header>
    <!-- Override Spring Boot version for 4.x compatibility -->
    <version.spring-boot>4.0.7</version.spring-boot>
    <!-- Spring Framework 7.x is required for Spring Boot 4.0 -->
    <version.spring>7.0.8</version.spring>
    <!-- JUnit 6.x is required for Spring Boot 4.0 compatibility -->
    <version.junit>6.0.3</version.junit>
    <spring-boot-starter-camunda-sdk.sourcedir>${project.basedir}/../spring-boot-starter-camunda-sdk/src</spring-boot-starter-camunda-sdk.sourcedir>
  </properties>

  <dependencies>
    <!-- Base Spring Boot starter - will be shaded -->
    <dependency>
      <groupId>io.camunda</groupId>
      <artifactId>spring-boot-starter-camunda-sdk</artifactId>
    </dependency>

    <dependency>
      <groupId>org.springframework</groupId>
      <artifactId>spring-beans</artifactId>
    </dependency>
    <dependency>
      <groupId>io.micrometer</groupId>
      <artifactId>micrometer-core</artifactId>
      <optional>true</optional>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-autoconfigure</artifactId>
    </dependency>
    <!-- New in Spring Boot 4: Jackson module is separate -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-jackson</artifactId>
    </dependency>
    <!-- New in Spring Boot 4: Health module is separate -->
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-health</artifactId>
    </dependency>

    <!-- Test dependencies -->
    <dependency>
      <groupId>com.auth0</groupId>
      <artifactId>java-jwt</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.springframework</groupId>
      <artifactId>spring-test</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.wiremock</groupId>
      <artifactId>wiremock-standalone</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.assertj</groupId>
      <artifactId>assertj-core</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.mockito</groupId>
      <artifactId>mockito-junit-jupiter</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.mockito</groupId>
      <artifactId>mockito-core</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-test</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter-api</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.junit.jupiter</groupId>
      <artifactId>junit-jupiter-params</artifactId>
      <scope>test</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-compiler-plugin</artifactId>
        <configuration>
          <compilerArgs>
            <arg>-parameters</arg>
          </compilerArgs>
        </configuration>
      </plugin>

      <!-- Build this module's own sources jar before the package phase, so the shade plugin
           (createSourcesJar) can merge it with the base module sources. Reuses the release
           parent's "attach-sources" execution id to move it from package to prepare-package. -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-source-plugin</artifactId>
        <executions>
          <execution>
            <id>attach-sources</id>
            <goals>
              <goal>jar-no-fork</goal>
            </goals>
            <phase>prepare-package</phase>
            <!-- Build the sources jar on disk for shade (createSourcesJar) to consume, but do not
                 attach it: the deploy command's source:jar already attaches the sources artifact and
                 shade attaches the shaded one. Attaching here too triggers "duplicated artifacts
                 attached" under maven-source-plugin < 3.4.0. -->
            <configuration>
              <attach>false</attach>
            </configuration>
          </execution>
        </executions>
      </plugin>

      <!-- Shade plugin to include and relocate classes from spring-boot-starter-camunda-sdk -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-shade-plugin</artifactId>
        <executions>
          <execution>
            <goals>
              <goal>shade</goal>
            </goals>
            <phase>package</phase>
            <configuration>
              <createDependencyReducedPom>true</createDependencyReducedPom>
              <!-- Also shade the sources jar so it carries the base module sources plus our
                   overrides, instead of only this module's few files (see #48090). -->
              <createSourcesJar>true</createSourcesJar>
              <promoteTransitiveDependencies>true</promoteTransitiveDependencies>
              <artifactSet>
                <includes>
                  <!-- Only include the base starter module in the shaded jar -->
                  <include>io.camunda:spring-boot-starter-camunda-sdk</include>
                </includes>
              </artifactSet>
              <filters>
                <filter>
                  <artifact>io.camunda:spring-boot-starter-camunda-sdk</artifact>
                  <excludes>
                    <!-- Exclude classes that we override with Spring Boot 4 versions -->
                    <exclude>io/camunda/zeebe/spring/client/actuator/ZeebeClientHealthIndicator.class</exclude>
                    <exclude>io/camunda/zeebe/spring/client/configuration/ZeebeActuatorConfiguration.class</exclude>
                    <exclude>io/camunda/zeebe/spring/client/configuration/ZeebeClientProdAutoConfiguration.class</exclude>
                    <exclude>io/camunda/zeebe/spring/client/configuration/CamundaAutoConfiguration.class</exclude>
                    <exclude>io/camunda/zeebe/spring/client/configuration/ExecutorServiceConfiguration.class</exclude>
                    <!-- The same overrides in the shaded sources jar (createSourcesJar). Filters
                         match by literal path, so the .class excludes above don't cover .java. -->
                    <exclude>io/camunda/zeebe/spring/client/actuator/ZeebeClientHealthIndicator.java</exclude>
                    <exclude>io/camunda/zeebe/spring/client/configuration/ZeebeActuatorConfiguration.java</exclude>
                    <exclude>io/camunda/zeebe/spring/client/configuration/ZeebeClientProdAutoConfiguration.java</exclude>
                    <exclude>io/camunda/zeebe/spring/client/configuration/CamundaAutoConfiguration.java</exclude>
                    <exclude>io/camunda/zeebe/spring/client/configuration/ExecutorServiceConfiguration.java</exclude>
                    <!-- Exclude META-INF services that we override -->
                    <exclude>META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports</exclude>
                  </excludes>
                </filter>
              </filters>
              <transformers>
                <!-- Merge service files -->
                <transformer implementation="org.apache.maven.plugins.shade.resource.ServicesResourceTransformer"/>
                <!-- Keep manifest entries -->
                <transformer implementation="org.apache.maven.plugins.shade.resource.ManifestResourceTransformer"/>
              </transformers>
            </configuration>
          </execution>
        </executions>
      </plugin>

      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-dependency-plugin</artifactId>
        <configuration>
          <!--
            Ignore all "used but undeclared" dependencies in this module.
            This starter shades and reuses classes and tests from the base
            spring-boot-starter-camunda-sdk module, which is where the actual
            dependency declarations live. From the perspective of this POM,
            many classes therefore appear as used but undeclared, even though
            they are intentionally provided by the base module and by the
            shaded artifact.
          -->
          <ignoredUsedUndeclaredDependencies>
            <dependency>*</dependency>
          </ignoredUsedUndeclaredDependencies>
          <!-- Ignore any non-test scoped deps, likely false-positive -->
          <ignoredNonTestScopedDependencies>
            <dependency>*</dependency>
          </ignoredNonTestScopedDependencies>
        </configuration>
      </plugin>

      <!-- Add test sources from the base module -->
      <plugin>
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>build-helper-maven-plugin</artifactId>
        <executions>
          <execution>
            <id>add-test-source</id>
            <goals>
              <goal>add-test-source</goal>
            </goals>
            <phase>generate-test-sources</phase>
            <configuration>
              <sources>
                <source>${spring-boot-starter-camunda-sdk.sourcedir}/test/java</source>
              </sources>
            </configuration>
          </execution>
          <execution>
            <id>add-test-resources</id>
            <goals>
              <goal>add-test-resource</goal>
            </goals>
            <phase>generate-test-resources</phase>
            <configuration>
              <resources>
                <resource>
                  <directory>${spring-boot-starter-camunda-sdk.sourcedir}/test/resources</directory>
                </resource>
              </resources>
            </configuration>
          </execution>
        </executions>
      </plugin>

      <!-- Run tests compiled from base module sources -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
      </plugin>
    </plugins>
  </build>
</project>
