---

# overwrites the default elasticsearch connection security
# properties in Optimize
es:
  connection:
    nodes:
      - host: 'localhost'
        httpPort: 9202
  security:
    username: 'elastic'
    password: 'optimize'
    ssl:
      enabled: false
container:
  checkMetadata: false