---

# overwrites the default elasticsearch connection security
# properties in Optimize
es:
  connection:
    nodes:
      - host: 'localhost'
        httpPort: 9201
  security:
    ssl:
      certificate: 'certs/optimize/optimize.crt'
      certificate_authorities: ['certs/ca/ca.crt']
      enabled: true