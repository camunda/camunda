---
plugin:
  directory: '.'

engines:
  'camunda-bpm':
    name: default
    rest: '${OPTIMIZE_CAMUNDABPM_REST_URL:http://localhost:8080/engine-rest}'
    # we control the import manually from tests
    importEnabled: false
    authentication:
      enabled: false