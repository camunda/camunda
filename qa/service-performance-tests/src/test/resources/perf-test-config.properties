 data generation
#data.generation.numberOfThreads=${data.generation.numberOfThreads}
#data.generation.size=${data.generation.size}

# elasticsearch
camunda.optimize.es.index=optimize
camunda.optimize.es.event.type=event
camunda.optimize.es.branchAnalysisData.type=branchAnalysisData
camunda.optimize.serialization.date.format=yyyy-MM-dd'T'HH:mm:ss

# optimize
camunda.optimize.rest.heatmap.frequency=http://localhost:8090/api/process-definition/heatmap/frequency
camunda.optimize.rest.heatmap.duration=http://localhost:8090/api/process-definition/heatmap/duration

# tests
#test.heatmap.max.duration.ms=${test.heatmap.max.duration.ms}

camunda.optimize.engine.enabled=false
