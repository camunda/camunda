identity.currentVersion.docker=${version.identity.docker.current}
#zeebe.currentVersion.docker=${version.zeebe.docker.current} - need to move this back - commenting temporarily as the image tag is different than the version
zeebe.currentVersion.docker=SNAPSHOT
