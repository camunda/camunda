query getTasks($query: TaskQuery!){
    tasks(query: $query) {
        id
        name
        processName
        creationTime
        completionTime
        assignee {
            username
            firstname
            lastname
        }
        variables {
            ... variableFields
        }
        taskState
        sortValues
        isFirst
        formKey
        processDefinitionId
    }
}
