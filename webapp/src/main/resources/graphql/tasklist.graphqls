# A task...
type Task {
    # the unique identifier of the task
    key: ID!
}

type User {
    username: ID!
    firstname: String
    lastname: String
}

type Query {
    # find all tasks
    tasks: [Task!]!
    currentUser: User!
}