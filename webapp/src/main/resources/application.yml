
spring:
  config:
    activate:
      on-profile: "identity-auth"
  security:
    oauth2:
      resourceserver:
        jwt:
          issuer-uri: ${camunda.tasklist.identity.issuerUrl}
---
spring:
  config:
    activate:
      on-profile: "dev"
graphql:
  tools:
    introspection-enabled: true
