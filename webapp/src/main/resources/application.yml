spring:
  config:
    activate:
      on-profile: "dev"
graphql:
  tools:
    introspection-enabled: true
camunda:
  tasklist:
    csrf-prevention-enabled: ${camunda.tasklist.csrfPreventionEnabled:true}