# Operate Dashboard: Carbon → Design System component tiering

**Sources of truth**, in priority order per the DS plugin's own docs:

1. `skills/migrate/scripts/data/mapping.json` (machine-readable bucket assignments — authoritative when it disagrees with the KB doc below)
2. `docs/kb/carbon-migration-tiers.md` (tier definitions, naming rules, escalation rules)
3. `carbon-compat/MAPPING.md` (per-component prop transforms)
4. `docs/kb/carbon-icons-to-lucide.md` (icon mapping)

All four live in the `camunda-design-system` Claude Code plugin, every claim below is also checked against this repo's own installed copy so a reviewer without the plugin can verify it.

- `webapp/client/node_modules/@camunda/design-system` is on `0.61.1`
- every `carbon-compat` source snippet quoted below is copied verbatim from that installed copy (`dist/carbon-compat/*.js` / `*.d.ts`), not from the plugin repo.
- Symbol list confirmed exhaustive by `grep -rn "@carbon" src/operate/pages/Dashboard src/operate/components/InstancesBar src/operate/components/EmptyState` (excluding tests) on 2026-09-16.

## Component tiering

| Carbon symbol                                                                             | File(s)                                                                                                    | Tier (mapping.json)       | Verdict for this page                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                              | Consuming PR                                 |
| ----------------------------------------------------------------------------------------- | ---------------------------------------------------------------------------------------------------------- | ------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------- |
| `Tile`                                                                                    | `Dashboard/styled.ts`                                                                                      | REMAP → `Card`            | **Dropped, not REMAPped.** mapping.json's generic bucket is `Card`, but PR 2's shell doesn't use `Card` anywhere: a `Card` frame isn't part of what any of these three regions actually are, and would just get torn out again once their real content lands. The metric panel renders as a plain `div` (its eventual title uses the DS top-level `Heading` component instead); the two list columns render DS `DataTable` directly, whose own `title` prop replaces the `Card`/`CardHeader`/`CardTitle` wrapper. No component here ends up on `Card`.                                                                                                             | PR 2                                         |
| `DataTable` (render-prop)                                                                 | `PartiallyExpandableDataTable/PartiallyExpandableDataTable.tsx`                                            | `shim` (SWAP sub-type)    | **Does not apply here.** Verbatim from the installed `carbon-compat/data-table.d.ts`: "Carbon's render-props `children` pattern is NOT supported — the DS DataTable uses a declarative API. Consumers still using render-props must migrate to DS columns/data. A dev-mode warning fires if children are supplied." The type itself marks it `/** @deprecated render-props children are not supported — use DS DataTable props directly. */ children?: unknown;`. This call site is 100% render-props (`children={({rows, headers, ...}) => ...}`). Full rewrite against native DS `DataTable`'s `columns`/`data`/`expansion` props — see open question below.     | PR 5                                         |
| `TableExpandHeader`, `TableHeader`, `TableRow`                                            | `PartiallyExpandableDataTable/PartiallyExpandableDataTable.tsx`                                            | `shim`                    | Superseded, not adopted. `carbon-compat/table-expand.js`'s own docstring: "Prefer enabling `expansion` on DS DataTable instead." Row expansion becomes the `expansion` prop on the rewritten table.                                                                                                                                                                                                                                                                                                                                                                                                                                                                | PR 5                                         |
| `Table`, `TableContainer`, `TableCell`, `TableHead`, `TableExpandRow`, `TableExpandedRow` | `PartiallyExpandableDataTable/styled.ts`                                                                   | `shim`/`swap`             | Same as above — the whole file is replaced by the PR 5 rewrite, not restyled in place.                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                                             | PR 5                                         |
| `DataTableSkeleton`                                                                       | `ExpandableList.tsx`                                                                                       | `swap`                    | Superseded by DS `Skeleton`, already how PR 5 is scoped (matches the DS DataTable being adopted in the same PR — no reason to bring in a second, Carbon-shaped skeleton).                                                                                                                                                                                                                                                                                                                                                                                                                                                                                          | PR 5                                         |
| `InlineLoading`                                                                           | `ExpandableList.tsx`, `InstancesByProcess/InstancesByProcess.tsx`, `IncidentsByError/IncidentsByError.tsx` | `shim`                    | **Caveat, not a straight SWAP.** The installed `carbon-compat/inline-loading.js` is, in full: `// MIGRATION TODO: adapter pending Loading shadcn component` / `export { InlineLoading } from '@carbon/react';` — not an adapter, a bare re-export of the real Carbon component. `src/routes/_shadcn/route.tsx` loads `tailwind.css` and the DS `ThemeProvider`, not Carbon's SCSS — so a component that still is Carbon, rendered under that route tree, has no stylesheet backing it and would render unstyled. Do not import it; replace with DS `Skeleton` as part of the PR 5 rewrite (all three call sites are inside the list being rewritten there anyway). | PR 5                                         |
| `Tooltip`                                                                                 | `InstancesBar/InstancesBar.tsx`                                                                            | `swap`                    | Genuine SWAP candidate — real adapter (`carbon-compat/tooltip.js`, wraps DS's own Radix tooltip primitives), API-compatible, call site otherwise unchanged. **Related decision noted below**, since it would be the first `carbon-compat` import in this repo — not gating this issue.                                                                                                                                                                                                                                                                                                                                                                             | PR 4                                         |
| `Button`, `Link`, `Stack`                                                                 | `EmptyState/EmptyState.tsx`                                                                                | `swap`                    | Moot rather than declined: `EmptyState/EmptyState.tsx` itself is replaced wholesale by native DS `EmptyState` (PR 3), whose own action-button API absorbs what `Button`/`Link` did here. `Stack`'s layout role is replaced by the DS component's internal layout. None of the three get individually imported anywhere post-migration.                                                                                                                                                                                                                                                                                                                             | PR 3                                         |
| `styles` (`@carbon/type`)                                                                 | `Dashboard/styled.ts`, `MetricPanel/styled.ts`, `InstancesBar/styled.ts`, `EmptyState/styled.tsx`          | not a component — no tier | Typography tokens (e.g. `productiveHeading02`), not swappable 1:1. Replace with Tailwind typography utilities per `docs/specs/shadcn/tokens-and-layout.md`, or the DS `Text` component where one exists for the semantic role. Each `shadcn.components/` file re-derives its own classes rather than importing this.                                                                                                                                                                                                                                                                                                                                               | Whichever PR rewrites that file (2, 4, 6, 3) |

## Icon mapping

| Carbon icon | Import                                                                                                          | Lucide equivalent                           | Notes                                                                                                                                                                                                                                                                                                                                                                                                                     |
| ----------- | --------------------------------------------------------------------------------------------------------------- | ------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- |
| `Timer`     | `InstancesBar/InstancesBar.tsx` (`@carbon/react/icons`), used at 16px in the draining-instances tooltip trigger | `Timer` from `@camunda/design-system/icons` | Identical name, confirmed present in the installed `lucide-react` version DS re-exports (`timer.mjs`). Not in the KB's own "trivial" table (that table is illustrative, not exhaustive) but the same-name case the table's rule covers. `size={16}` → `className="h-4 w-4"`; `aria-hidden="true"` and `focusable="false"` — `focusable` is a Carbon/SVG attribute with no Lucide equivalent, drop it; keep `aria-hidden`. | PR 4 |

No other icons are imported anywhere in the Dashboard subtree.

## Open question for DS-team review

**Sign-off on the `PartiallyExpandableDataTable` → DS `DataTable` rewrite.**

`carbon-compat`'s `DataTable` adapter explicitly refuses the render-prop pattern this component is built on (`children?: unknown`, `@deprecated`, dev-mode warning). [#59208](https://github.com/camunda/camunda/issues/59208) listed the render-prop `DataTable` as an open question pending DS-team sign-off; the Dashboard hits that exact gap in both `InstancesByProcess` and `IncidentsByError`. PR 5 needs confirmation that composing native DS `DataTable` with `columns`/`data`/`expansion` is the right target before that rewrite starts.

The Dashboard currently paginates by scroll, not by a pager — `useDashboardScrollPagination` drives `onScroll` on a wrapping container. That approach is dropped, not attempted-and-fallback: the DS docs list container-wrapping as an explicit anti-pattern for `DataTable` — its own `Table` wrapper already owns the scroll region, so nesting another scrollable ancestor around it produces double scrollbars. The open question is which of two replacements fits: (1) manual pagination with `pageCount: -1` (TanStack "infinite" mode) driven by a Load-more/Next button, or (2) if scroll-triggered loading is required, an `IntersectionObserver` on a sentinel below the table, triggered by the page's own natural scroll rather than a wrapper around the table. PR 5 should record which one is confirmed and why.

**Sanctioning `carbon-compat` for `Tooltip`.**

`InstancesBar`'s `Tooltip` is the first call site in this repo that would import from `carbon-compat` rather than the native DS component — every other row above either drops the symbol or migrates to a native DS API directly. The adapter (`carbon-compat/tooltip.js`) is a real wrapper over DS's own Radix tooltip primitives and is API-compatible with the existing call site, so it's a mechanically safe SWAP; what's open is whether `carbon-compat` imports are sanctioned for use in this codebase at all, since adopting one here sets precedent for later PRs. Not gating this issue (`carbon-compat/MAPPING.md` describes it as a like-for-like shim), but PR 4 needs a DS-team or tech-lead decision on this before it lands.
