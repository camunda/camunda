{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "unordered-list",
  "type": "registry:ui",
  "title": "UnorderedList",
  "description": "A styled `<ul>` with consistent disc bullets, indentation, and item spacing. Pairs with `ListItem` (which renders an `<li>`).",
  "dependencies": [],
  "files": [
    {
      "path": "components/ui/unordered-list.tsx",
      "type": "registry:ui",
      "content": "/*\n * Copyright Camunda Services GmbH and/or licensed to Camunda Services GmbH under\n * one or more contributor license agreements. See the NOTICE file distributed\n * with this work for additional information regarding copyright ownership.\n * Licensed under the Camunda License 1.0. You may not use this file\n * except in compliance with the Camunda License 1.0.\n */\n\nimport * as React from 'react';\n\nimport {cn} from '@/lib/utils';\n\ntype UnorderedListProps = React.ComponentPropsWithoutRef<'ul'> & {\n  nested?: boolean;\n  isExpressive?: boolean;\n};\n\nfunction UnorderedList({\n  className,\n  nested,\n  isExpressive,\n  ...props\n}: UnorderedListProps) {\n  return (\n    <ul\n      data-slot=\"unordered-list\"\n      data-nested={nested ? 'true' : undefined}\n      data-expressive={isExpressive ? 'true' : undefined}\n      className={cn(\n        'list-disc pl-5 text-sm leading-6 marker:text-muted-foreground',\n        nested && 'mt-1 mb-0 ml-4',\n        isExpressive && 'text-base leading-7',\n        className,\n      )}\n      {...props}\n    />\n  );\n}\n\ntype ListItemProps = React.ComponentPropsWithoutRef<'li'>;\n\nfunction ListItem({className, ...props}: ListItemProps) {\n  return (\n    <li\n      data-slot=\"list-item\"\n      className={cn('mt-1 first:mt-0', className)}\n      {...props}\n    />\n  );\n}\n\nexport {UnorderedList, ListItem};\nexport type {UnorderedListProps, ListItemProps};\n"
    }
  ],
  "meta": {
    "flavor": "shadcn",
    "package": "@camunda/design-system",
    "packageSubpath": "@camunda/design-system/shadcn",
    "import": "import {UnorderedList} from '@camunda/design-system/shadcn';",
    "docs": "# UnorderedList\n\nA styled `<ul>` with consistent disc bullets, indentation, and item spacing. Pairs with `ListItem` (which renders an `<li>`).\n\n## When to use\n\n- **Bulleted prose lists** — release notes, requirements, \"what's included\" sections.\n- **Nested lists** — pass `nested` on the inner `UnorderedList` to remove the disc duplication and tighten spacing.\n- **Expressive copy** — pass `isExpressive` for slightly larger type with more leading.\n\n## When not to use\n\n- **Numbered steps** — render `<ol>` directly (or add an `OrderedList` wrapper if you need one).\n- **Navigation** — use a `<nav>` element with anchors instead.\n- **Selectable rows** — see `ContainedList` or `Table`.\n\n## Composition\n\n- Both components are pure HTML wrappers — no JS, no portals.\n- Tailwind: `list-disc pl-5 text-sm leading-6`. `nested` adds `mt-1 mb-0 ml-4`. `isExpressive` switches to `text-base leading-7`.\n- Override with `className` on either component when a specific list needs different rhythm.",
    "migrationGuide": "# UnorderedList — Carbon → shadcn migration guide\n\nA 1-to-1 functional match. Carbon's `UnorderedList` and `ListItem` map to plain `<ul>` and `<li>` with Tailwind list utilities.\n\n## Mapping\n\n| Carbon prop | shadcn prop | Notes |\n|---|---|---|\n| `nested` | `nested` | On the inner list. Removes outer margin and pulls indentation in. |\n| `isExpressive` | `isExpressive` | Larger type / more leading. |\n| `className` | `className` | Forwarded. |\n| any `<ul>` HTML attr | same | Forwarded. |\n\n## Carbon features missing in shadcn\n\n- **IBM type-token alignment** — Carbon uses `body-compact-01` / `body-01` tokens. shadcn uses Tailwind's `text-sm` / `text-base`. Override via `className` if exact parity matters.\n- **`OrderedList`** — not part of this migration (no Camunda usage). Use `<ol>` directly with the same Tailwind list classes.\n\n## shadcn features missing in Carbon\n\n- **`data-nested` and `data-expressive`** attributes for CSS hooks.\n\n## Behavioural differences\n\n- **Bullet colour** — Carbon: matches text colour. shadcn: `marker:text-muted-foreground`. Override via `className` if you want bullets to match the text.\n- **Margin between items** — Carbon: `0.5rem`. shadcn: Tailwind `mt-1` (`0.25rem`). Tighten or loosen with `className` on the `<li>`.\n\n## Migration checklist\n\n1. Swap `import {UnorderedList, ListItem} from '@carbon/react'` → `from '@camunda/design-system/shadcn'`.\n2. Keep `nested` and `isExpressive` props as-is.\n3. If you need bullet/text colour parity with Carbon, add `marker:text-foreground` to the outer list.\n4. Drop `@use '@carbon/styles/scss/components/list'` imports."
  }
}
