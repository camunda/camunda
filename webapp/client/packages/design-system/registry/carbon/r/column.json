{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "column",
  "type": "registry:ui",
  "title": "Column",
  "description": "A child of `Grid`. Sets `grid-column` for itself based on either a constant `span`, or breakpoint-specific spans (`sm` / `md` / `lg` / `xl` / `max`). Supports `offset`, `start`, and `end` for fine-grained placement.",
  "dependencies": [
    "@carbon/react"
  ],
  "files": [
    {
      "path": "components/ui/column.tsx",
      "type": "registry:ui",
      "content": "/*\n * Copyright Camunda Services GmbH and/or licensed to Camunda Services GmbH under\n * one or more contributor license agreements. See the NOTICE file distributed\n * with this work for additional information regarding copyright ownership.\n * Licensed under the Camunda License 1.0. You may not use this file\n * except in compliance with the Camunda License 1.0.\n */\n\nexport {Column} from '@carbon/react';\n"
    }
  ],
  "meta": {
    "flavor": "carbon",
    "package": "@camunda/design-system",
    "packageSubpath": "@camunda/design-system/carbon",
    "import": "import {Column} from '@camunda/design-system/carbon';",
    "docs": "# Column\n\nA child of `Grid`. Sets `grid-column` for itself based on either a constant `span`, or breakpoint-specific spans (`sm` / `md` / `lg` / `xl` / `max`). Supports `offset`, `start`, and `end` for fine-grained placement.\n\n## When to use\n\n- Inside `<Grid>`, when you need a layout cell that spans some number of columns.\n\n## When not to use\n\n- Outside a `<Grid>`. The component reads column count from grid context.\n\n## Composition\n\n- `span` accepts a number (1–`columns`), a boolean (`true` = full width, `false` = auto), or an object `{ span, offset, start, end }`.\n- `sm` / `md` / `lg` / `xl` / `max` accept the same shape and apply at-or-above the breakpoint min-width:\n  - `sm` ≥ 0px\n  - `md` ≥ 672px\n  - `lg` ≥ 1056px\n  - `xl` ≥ 1312px\n  - `max` ≥ 1584px\n- A constant `span` is applied as an inline `style.gridColumn`. Breakpoint props inject a small scoped `<style>` element with media queries — this is necessary because Tailwind cannot generate arbitrary breakpoint utilities at runtime.",
    "migrationGuide": "# Column — Carbon → shadcn migration guide\n\n`Column` is the cell primitive that pairs with `Grid`. The shadcn version preserves Carbon's prop shape including the `{ span, offset, start, end }` object form and breakpoint props.\n\n## Mapping\n\n| Carbon prop | shadcn prop | Notes |\n|---|---|---|\n| `span` | `span` | `number`, `boolean`, or object form. |\n| `sm` | `sm` | Breakpoint span. ≥ 0px. |\n| `md` | `md` | ≥ 672px. |\n| `lg` | `lg` | ≥ 1056px. |\n| `xlg` | `xl` | ≥ 1312px. **Renamed.** |\n| `max` | `max` | ≥ 1584px. |\n| `as` | `as` | Polymorphic. Defaults to `<div>`. |\n| `'25%' \\| '50%' \\| '75%' \\| '100%'` | percentage strings | **Not supported.** Convert to numeric span (16-col grid). |\n\n## Carbon features missing in shadcn\n\n- **Percentage spans** (`span=\"50%\"`) — convert to numeric (`span={8}` on 16-col grid).\n- **`ColumnHang`** — hanging-column primitive. Not provided. Use `start`/`end` or a negative-margin override.\n- **Carbon's exact gutter behaviour at the `<Column>` level** — Carbon distributes gutter via SCSS variables; shadcn relies on the parent grid's `gap`.\n\n## shadcn features missing in Carbon\n\n- **`xl`** — clearer name than Carbon's `xlg`.\n\n## Behavioural differences\n\n- **Implementation strategy** — shadcn emits a small `<style>` element next to the column when responsive props are set. Tailwind cannot generate arbitrary `min-[Npx]:[grid-column:...]` classes at runtime, so an inline media query block is the safest solution.\n- **`auto` placement** — pass `span={false}` for content-sized columns. Same as Carbon's `false`.\n- **Offset** — `offset={n}` shifts the column right by `n` tracks. Same as Carbon.\n\n## Migration checklist\n\n1. Swap the import.\n2. Rename every `xlg={…}` to `xl={…}`.\n3. Replace any percentage spans with numeric equivalents (e.g. `'50%'` → `8` on a 16-col grid).\n4. If you used `ColumnHang`, refactor to `start`/`end` or wrap with a negative-margin div.\n5. Drop `@use '@carbon/styles/scss/grid'` imports."
  }
}
