{
  "$schema": "https://ui.shadcn.com/schema/registry-item.json",
  "name": "section",
  "type": "registry:ui",
  "title": "Section",
  "description": "Pairs with `Heading` to deliver an outline-correct, context-aware document hierarchy. Each `Section` increments a heading level provided through React context; `Heading` reads that level and renders the matching `<h1>` … `<h6>` element automatically.",
  "dependencies": [],
  "files": [
    {
      "path": "components/ui/section.tsx",
      "type": "registry:ui",
      "content": "/*\n * Copyright Camunda Services GmbH and/or licensed to Camunda Services GmbH under\n * one or more contributor license agreements. See the NOTICE file distributed\n * with this work for additional information regarding copyright ownership.\n * Licensed under the Camunda License 1.0. You may not use this file\n * except in compliance with the Camunda License 1.0.\n */\n\nimport * as React from 'react';\n\nimport {cn} from '../../../lib/utils';\n\ntype HeadingLevel = 1 | 2 | 3 | 4 | 5 | 6;\n\nconst SectionContext = React.createContext<HeadingLevel>(1);\n\nfunction clamp(level: number): HeadingLevel {\n  if (level < 1) return 1;\n  if (level > 6) return 6;\n  return level as HeadingLevel;\n}\n\nfunction useSectionLevel() {\n  return React.useContext(SectionContext);\n}\n\ntype SectionProps<E extends React.ElementType> = {\n  as?: E;\n  level?: HeadingLevel;\n} & Omit<React.ComponentPropsWithoutRef<E>, 'as'>;\n\nfunction Section<E extends React.ElementType = 'section'>({\n  as,\n  level: explicitLevel,\n  className,\n  children,\n  ...props\n}: SectionProps<E>) {\n  const parentLevel = useSectionLevel();\n  const level = clamp(explicitLevel ?? parentLevel + 1);\n  const Comp = (as ?? 'section') as React.ElementType;\n  return (\n    <SectionContext.Provider value={level}>\n      <Comp data-slot=\"section\" className={cn(className)} {...props}>\n        {children}\n      </Comp>\n    </SectionContext.Provider>\n  );\n}\n\nexport {Section, SectionContext, useSectionLevel};\nexport type {HeadingLevel, SectionProps};\n"
    }
  ],
  "meta": {
    "package": "@camunda/design-system",
    "import": "import {Section} from '@camunda/design-system';",
    "docs": "# Section\n\nPairs with `Heading` to deliver an outline-correct, context-aware document hierarchy. Each `Section` increments a heading level provided through React context; `Heading` reads that level and renders the matching `<h1>` … `<h6>` element automatically.\n\n## When to use\n\n- **Reusable content blocks** — drop a chunk of UI somewhere different in the page tree and let it pick up the right heading depth without rewriting tags.\n- **Outlines that mirror the visual structure** — preserves a valid heading hierarchy for screen readers and SEO without manual `h2`/`h3` bookkeeping.\n- **Polymorphic semantics** — pass `as=\"article\"`, `as=\"aside\"`, etc. when `<section>` isn't the right element; the level context still cascades.\n\n## When not to use\n\n- **Page hero or top-of-page title** — render a plain `<h1>` directly. There is no level cascade to manage at the top.\n- **Visual-only emphasis** — if you only want big text, use the typography utility classes (e.g. `text-4xl font-semibold`) on a `<span>` or `<p>`.\n\n## Composition\n\n- `<Section>` is polymorphic via `as`; defaults to `<section>`.\n- `level` overrides the auto-incremented level — the next nested `<Heading>` will use the override and the next `<Section>` will increment from there.\n- The cascade only affects descendants. Headings outside of any `<Section>` default to `<h1>`.",
    "migrationGuide": "# Section — Carbon → shadcn migration guide\n\nCarbon ships `Section` and `Heading` as a paired primitive that uses React context to auto-level headings. The shadcn wrapper preserves the same shape: drop-in identical export names, identical mental model, only the underlying element is plain HTML + Tailwind instead of Carbon SCSS.\n\n## Mapping\n\n| Carbon prop | shadcn prop | Notes |\n|---|---|---|\n| `as` | `as` | Polymorphic element. Defaults to `<section>`. |\n| `level` | `level` | Forces the heading level for descendants (1–6). Auto-clamped if out of range. |\n| `className` | `className` | Forwarded to the rendered element. |\n| children | children | Headings inside descend through the level context. |\n\n## Carbon features missing in shadcn\n\n- **Carbon-styled section padding/spacing tokens** — Carbon's `Section` ships a small amount of layout polish via SCSS. shadcn's `Section` is unstyled; add Tailwind utilities (`py-6`, `space-y-4`, etc.) yourself.\n- **`Heading` typography preset that exactly matches IBM's type scale** — shadcn uses Tailwind's `text-4xl` → `text-base` ramp. Visually similar but not pixel-identical.\n\n## shadcn features missing in Carbon\n\n- None — the shadcn wrapper is intentionally a 1-to-1 functional match.\n\n## Behavioural differences\n\n- **No `<section>` reset CSS** — Carbon's SCSS resets margin/padding for inner headings; shadcn relies on Tailwind's preflight which already resets headings.\n- **`useSectionLevel` hook is exported** — read the current level inside custom components without rendering a `Heading`. Carbon doesn't expose this hook publicly.\n\n## Migration checklist\n\n1. `import {Section} from '@camunda/design-system/shadcn'` (instead of `'@carbon/react'`).\n2. Same for `Heading` (it lives in the `heading` wrapper).\n3. Keep `as`, `level`, and children unchanged.\n4. Audit your typography: shadcn applies `text-4xl/3xl/2xl/xl/lg/base font-semibold tracking-tight` for h1 → h6. Override via `className` if a section needs different scale.\n5. Drop any `@use '@carbon/styles/scss/components/heading'` imports — they're not needed."
  }
}
