---
import:
  handler:
    backoff:
      interval: 50
      value: 10
      max: 3
    pages:
      resetInterval:
        unit: Millis
        value: 100
container:
  checkMetadata: false
es:
  ports:
    tcp: 9302
    http: 9200
  sampler:
    interval: 100

email:
  enabled: true