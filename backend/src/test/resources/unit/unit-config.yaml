---
import:
  handler:
    backoff:
      interval: 50
      value: 10
      max: 3
    pages:
      resetInterval:
        unit: Millis
        value: 100
container:
  checkMetadata: false
es:
  port: 9302
  sampler:
    interval: 100

email:
  enabled: true