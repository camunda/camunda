springfox.documentation.swagger.v2.path: /documentation
camunda.bpm.operate:
  elasticsearch:
    clusterName: docker-cluster
    host: localhost
    port: 9300
    dateFormat: yyyy-MM-dd'T'HH:mm:ss.SSSZ
    demoData: false
  zeebe:
    brokerContactPoint: localhost:51015
    demoData: true
    topics:
      - new-topic
    worker: operate
logging:
  level:
    ROOT: INFO
    org.camunda.operate: DEBUG
---
spring:
  profiles: dev
camunda.bpm.operate:
  elasticsearch:
    clusterName: docker-cluster
    host: elasticsearch
    port: 9300
    dateFormat: yyyy-MM-dd'T'HH:mm:ss.SSSZ
    demoData: false
  zeebe:
    brokerContactPoint: zeebe:51015
    demoData: true
    topics:
      - new-topic
    worker: operate
logging:
  level:
    ROOT: INFO
    org.camunda.operate: INFO