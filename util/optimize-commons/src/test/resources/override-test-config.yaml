---
auth:
  token:
    lifeMin: 10

container:
  keystore:
    location: override-test-config.yaml

es:
  security:
    username: ''
    password: ''
    ssl:
      key: ''
      certificate: ''
      certificate_authorities: ''
      verification_mode: ''
      enabled: true
