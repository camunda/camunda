---
security:
  auth:
    token:
      lifeMin: 10

container:
  keystore:
    location: override-test-config.yaml

es:
  security:
    username: ''
    password: ''
    ssl:
      certificate: ''
      certificate_authorities: ''
      enabled: true

isIt: true
