# This is optional (added via CLI) if login to Docker registry is required, e.g.,
# to avoid Docker Hub rate limits.
- op: add
  path: /spec/template/spec/imagePullSecrets/-
  value:
    name: index-docker-io
