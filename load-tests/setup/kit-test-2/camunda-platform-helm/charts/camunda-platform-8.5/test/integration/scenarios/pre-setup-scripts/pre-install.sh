#!/bin/bash
#
# This script will run before the Camunda Helm chart install step in the "install" flow (no upgrade in that flow).
# Any necessary tasks should be performed here and removed after the release.
#
