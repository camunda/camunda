#!!! DO NOT ADD CREDENTIALS TO THE REPO !!!
cloudcredentials.yaml
# Rendered per-namespace secrets (random credentials baked in by newLoadTest.sh).
# Pattern matches only namespace folders (always `c8-*`), not the default/ template.
c8-*/*.secrets.*
