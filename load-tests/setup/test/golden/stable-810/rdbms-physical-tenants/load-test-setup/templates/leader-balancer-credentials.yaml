---
# Source: load-test-setup/templates/leader-balancer-credentials.yaml
apiVersion: v1
kind: Secret
metadata:
  name: leader-balancer-credentials
type: Opaque
stringData:
  clientId: "leader-balancer"
  # Same password as the one we are configuring through Keycloak in
  # "camunda-credentials.yaml" for: "identity-leader-balancer-client-secret"
  clientSecret: "Kozb6.KojuFugi"
  # Non-secret connection settings, reused from the load-test client's configuration.
  zeebeRestAddress: "http://camunda:8080"
  authServer: "http://keycloak:18080/auth/realms/camunda-platform/protocol/openid-connect/token"
  authorizationAudience: "orchestration-api"
