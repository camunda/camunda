// Package common holds shared constants used across all collector modules.
package common

// Namespace is the top-level Prometheus namespace for all metrics exposed by
// this exporter.
const Namespace = "camunda_exporter"
