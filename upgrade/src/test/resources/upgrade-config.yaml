es:
  connection:
    nodes:
    - host: 'localhost'
      tcpPort: 9300
      httpPort: 9200

serialization:
  # the date format that is used in Optimize internally
  optimizeDateFormat: yyyy-MM-dd'T'HH:mm:ss.SSSZ