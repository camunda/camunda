---
- hosts: all

  gather_facts: False
  
  tasks:
      - name: Ensure that apt repository list is updated
        raw: sudo apt-get update
      - name: Ensure that python 2 is installed
        raw: sudo apt-get -y install python
