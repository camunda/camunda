io:
  camunda:
    process:
      test:
        connectors-docker-image-version: 8.7-SNAPSHOT

logging:
  level:
    root: info
    io:
      camunda:
        process:
          test: info
    # Hide default logging from Testcontainers
    org:
      testcontainers: warn
    tc: error
