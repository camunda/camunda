# Disable Create React App's "preflight" dependency check.
#
# This project is built via Maven (frontend-maven-plugin) in a large monorepo.
# Some developers might have a global/home-level ESLint installation which
# causes CRA v4 to fail the build.
#
# owner: TODO
SKIP_PREFLIGHT_CHECK=true

