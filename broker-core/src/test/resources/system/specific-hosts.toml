[gateway.network]
host = "gatewayHost"

[network.client]
host = "clientHost"

[network.management]
host = "managementHost"

[network.replication]
host = "replicationHost"

[network.subscription]
host = "subscriptionHost"
