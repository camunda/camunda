{
  "$comment": "Test-only registry for the required-permissions Spectral fixture. Loaded via SPECTRAL_RESOURCE_PERMISSIONS_REGISTRY env var by helpers.js. Deliberately small; does NOT mirror the production enum.",
  "resourcePermissions": {
    "PROCESS_DEFINITION": ["READ_PROCESS_DEFINITION", "CREATE_PROCESS_INSTANCE", "UPDATE_USER_TASK"],
    "USER_TASK": ["READ", "UPDATE", "COMPLETE"],
    "RESOURCE": ["DELETE_PROCESS"]
  }
}
