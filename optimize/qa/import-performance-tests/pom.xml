<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <parent>
    <groupId>io.camunda.optimize</groupId>
    <artifactId>qa</artifactId>
    <version>8.6.0-SNAPSHOT</version>
  </parent>

  <artifactId>import-performance-tests</artifactId>

  <properties>
    <skip.tests>true</skip.tests>
    <argLine>-Xmx2048m</argLine>
    <db.url />
    <zeebe.enabled>false</zeebe.enabled>
    <zeebe.partition.count>1</zeebe.partition.count>
    <zeebe.maximportpagesize>10000</zeebe.maximportpagesize>
    <data.process.definition.count>100</data.process.definition.count>
    <data.instance.count>1000000</data.instance.count>
    <import.timeout.in.minutes>240</import.timeout.in.minutes>
  </properties>

  <dependencies>
    <dependency>
      <groupId>io.camunda.optimize</groupId>
      <artifactId>optimize-backend</artifactId>
      <version>${project.version}</version>
      <scope>test</scope>
    </dependency>

    <dependency>
      <groupId>io.camunda.optimize</groupId>
      <artifactId>data-generation</artifactId>
      <version>${project.version}</version>
      <scope>test</scope>
    </dependency>

    <dependency>
      <groupId>org.postgresql</groupId>
      <artifactId>postgresql</artifactId>
      <version>${postgres.version}</version>
      <scope>test</scope>
    </dependency>

    <dependency>
      <groupId>com.h2database</groupId>
      <artifactId>h2</artifactId>
      <version>${h2.version}</version>
      <scope>test</scope>
    </dependency>

    <dependency>
      <groupId>io.camunda.optimize</groupId>
      <artifactId>optimize-backend</artifactId>
      <version>${project.version}</version>
      <type>test-jar</type>
      <scope>test</scope>
    </dependency>

    <dependency>
      <groupId>org.springframework</groupId>
      <artifactId>spring-test</artifactId>
      <scope>test</scope>
    </dependency>
    <dependency>
      <groupId>org.springframework.boot</groupId>
      <artifactId>spring-boot-test</artifactId>
      <version>${spring.boot.version}</version>
      <scope>test</scope>
    </dependency>

  </dependencies>

  <build>
    <testResources>
      <testResource>
        <directory>src/test/resources</directory>
        <filtering>true</filtering>
      </testResource>
    </testResources>

    <plugins>
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-surefire-plugin</artifactId>
        <configuration>
          <skipTests>${skip.tests}</skipTests>
          <systemPropertyVariables>
            <ZEEBE_ENABLED>${zeebe.enabled}</ZEEBE_ENABLED>
            <ZEEBE_PARTITION_COUNT>${zeebe.partition.count}</ZEEBE_PARTITION_COUNT>
            <ZEEBE_MAX_IMPORT_PAGE_SIZE>${zeebe.maximportpagesize}</ZEEBE_MAX_IMPORT_PAGE_SIZE>
            <DATA_PROCESS_DEFINITION_COUNT>${data.process.definition.count}</DATA_PROCESS_DEFINITION_COUNT>
            <DATA_INSTANCE_COUNT>${data.instance.count}</DATA_INSTANCE_COUNT>
            <IMPORT_TIMEOUT_IN_MINUTES>${import.timeout.in.minutes}</IMPORT_TIMEOUT_IN_MINUTES>
          </systemPropertyVariables>
        </configuration>
      </plugin>
    </plugins>
  </build>

  <profiles>
    <profile>
      <id>static-data-import-performance-test</id>
      <properties>
        <skip.tests>false</skip.tests>
      </properties>
      <build>
        <plugins>
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-surefire-plugin</artifactId>
            <configuration>
              <includes>**/*StaticDataTest.java</includes>
            </configuration>
          </plugin>
        </plugins>
      </build>
    </profile>
    <profile>
      <id>live-data-import-performance-test</id>
      <properties>
        <skip.tests>false</skip.tests>
      </properties>
      <build>
        <plugins>
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-surefire-plugin</artifactId>
            <configuration>
              <redirectTestOutputToFile>false</redirectTestOutputToFile>
              <includes>**/*LiveLoadTest.java</includes>
            </configuration>
          </plugin>
        </plugins>
      </build>
    </profile>
    <profile>
      <id>zeebe-data-import-performance-test</id>
      <properties>
        <skip.tests>false</skip.tests>
      </properties>
      <build>
        <plugins>
          <plugin>
            <groupId>org.apache.maven.plugins</groupId>
            <artifactId>maven-surefire-plugin</artifactId>
            <configuration>
              <redirectTestOutputToFile>false</redirectTestOutputToFile>
              <includes>**/*ZeebeDataTest.java</includes>
            </configuration>
          </plugin>
        </plugins>
      </build>
    </profile>
  </profiles>

</project>
