---
apiVersion: v2
name: preview-environments
appVersion: dev
description: A Helm chart to deploy a Operate preview environment to the Camunda-CI cluster managed by the Infrastructure team.
icon: https://console.cloud.camunda.io/favicon.ico
type: application
# Chart version; expected to follow Semantic Versioning (https://semver.org/)
version: 0.1.1

dependencies:
- name: camunda-platform
  # @helm-camunda-io references https://helm.camunda.io repository configured as helm-camunda-io in Argo CD
  repository: "@helm-camunda-io"
  version: 8.2.2
- name: infra-preview-environments-ingress
  repository: oci://registry.camunda.cloud/library
  version: 1.1.1
