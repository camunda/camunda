# MCP gateway configuration - using programmatic bean registration
# Spring AI auto-configuration is disabled in favor of custom McpServersConfiguration
spring:
  ai:
    mcp:
      server:
        enabled: false
        annotation-scanner:
          enabled: false
