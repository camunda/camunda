spring:
  security:
    oauth2:
      client:
        registration:
          oidcclient:
            client-id: oidcclient2
            client-secret: "secret2"
            authorization-grant-type: authorization_code
            redirect-uri: http://localhost:8080/login/oauth2/code/oidcclient2
            provider: oidcclient
            scope:
              - "openid"
              - "profile"
        provider:
          oidcclient:
            issuer-uri: http://auth.identity:9000
