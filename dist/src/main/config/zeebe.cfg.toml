# Zeebe broker configuration file

# Controls the number of nodes (including itself) this broker waits for until
# bootrapping. 
bootstrap = 1

# Networking configuration ----------------------------

# [network]
# host = "0.0.0.0"
# defaultSendBufferSize = "16MB"

# [network.client]
# host = "localhost"
# port = 51015
# sendBufferSize = "16MB"
# controlMessageBufferSize = "8MB"

# [network.management]
# host = "localhost"
# port = 51016
# sendBufferSize = "16MB"
# receiveBufferSize = "8MB"

# [network.replication]
# host = "localhost"
# port = 51017
# sendBufferSize = "16MB"

# Cluster Configuration -------------------------------

# [cluster]
# initialContactPoints = []

# [gossip]
# retransmissionMultiplier = 3
# probeInterval = 1000
# probeTimeout = 500
# probeIndirectNodes = 3
# probeIndirectTimeout = 1000
# suspicionMultiplier = 5
# syncTimeout = 3000
# joinTimeout = 1000
# joinInterval = 5000
# leaveTimeout = 1000
# maxMembershipEventsPerMessage = 32
# maxCustomEventsPerMessage = 8

# [raft]
# heartbeatInterval = 250
# electionInterval = 1000
# leaveTimeout = 1000

# System configuration ------------------------------------

[threads]
cpuThreadCount = 2
ioThreadCount = 2

# [metrics]
# reportingInterval = 5000
# directory = ""
# metricsFile = "metrics/zeebe.prom"

# Data Configuration ---------------------------------------

# [data]
# defaultSegmentSize = "512MB"
# directories = [ "data" ]
