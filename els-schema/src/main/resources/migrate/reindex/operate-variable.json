{
	"source": {
		"index": [
			"operate-variable_*"
		]
	},
	"dest": {
		"index": "operate-variable-${schema.version}_"
	},
	"script": {
		"lang": "painless",
		"source": "ctx._index = 'operate-variable-${schema.version}_' + (ctx._index.substring(ctx._index.indexOf('_') + 1, ctx._index.length()))"
	}
}
