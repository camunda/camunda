{
	"source": {
		"index": [
			"operate-batch-operation-${schema.previous_version}_*"
		]
	},
	"dest": {
		"index": "operate-batch-operation-${schema.version}_"
	},
	"script": {
		"lang": "painless",
		"source": "ctx._index = 'operate-batch-operation-${schema.version}_' + (ctx._index.substring(ctx._index.indexOf('_') + 1, ctx._index.length()))"
	}
}
