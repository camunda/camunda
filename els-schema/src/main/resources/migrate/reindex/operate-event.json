{
	"source": {
		"index": [
			"operate-event-${schema.previous_version}_*"
		]
	},
	"dest": {
		"index": "operate-event-${schema.version}_"
	},
	"script": {
		"lang": "painless",
		"source": "ctx._index = 'operate-event-${schema.version}_' + (ctx._index.substring(ctx._index.indexOf('_') + 1, ctx._index.length()))"
	}
}
