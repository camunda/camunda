{
	"source": {
		"index": [
			"operate-activity-instance_*"
		]
	},
	"dest": {
		"index": "operate-activity-instance-${schema.version}_"
	},
	"script": {
		"lang": "painless",
		"source": "ctx._index = 'operate-activity-instance-${schema.version}_' + (ctx._index.substring(ctx._index.indexOf('_') + 1, ctx._index.length()))"
	}
}
