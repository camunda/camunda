{
  "source": {
    "index": [
      "tasklist-variable-${schema.previous_version}_*"
    ]
  },
  "dest": {
    "index": "tasklist-variable-${schema.version}_"
  },
  "script": {
    "lang": "painless",
    "source": "ctx._index = 'tasklist-variable-${schema.version}_' + (ctx._index.substring(ctx._index.indexOf('_') + 1, ctx._index.length()))"
  }
}
