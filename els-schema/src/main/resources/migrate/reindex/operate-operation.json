{
	"source": {
		"index": [
			"operate-operation_*"
		]
	},
	"dest": {
		"index": "operate-operation-${schema.version}_"
	},
	"script": {
		"lang": "painless",
		"source": "ctx._index = 'operate-operation-${schema.version}_' + (ctx._index.substring(ctx._index.indexOf('_') + 1, ctx._index.length()))"
	}
}
