{
	"source": {
		"index": [
			"operate-list-view-${schema.previous_version}_*"
		]
	},
	"dest": {
		"index": "operate-list-view-${schema.version}_"
	},
	"script": {
		"lang": "painless",
		"source": "ctx._index = 'operate-list-view-${schema.version}_' + (ctx._index.substring(ctx._index.indexOf('_') + 1, ctx._index.length()))"
	}
}
